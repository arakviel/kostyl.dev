# kostyl.dev

**kostyl.dev** — це майбутній освітній ресурс з навчальними матеріалами по програмуванню.

🚧 **Статус**: В розробці.

Сайт знаходиться на етапі активного створення та наповнення контентом.

## Робота з озвучкою (TTS)

Для створення аудіо-версій контенту ми використовуємо `edge-tts`.

### 1. Підготовка тексту

Щоб прибрати зайві Markdown-конструкції та компоненти Docus, використовуйте скрипт:

```bash
python3 clean_tts.py content/path/to/file.md content/path/to/file.tts.txt
```

### 2. Генерація аудіо

Для озвучки українською мовою:

```bash
# Голос Ostap (Чоловічий)
edge-tts --voice uk-UA-OstapNeural --file content/path/to/file.tts.txt --write-media content/path/to/file.mp3

# Голос Polina (Жіночий)
edge-tts --voice uk-UA-PolinaNeural --file content/path/to/file.tts.txt --write-media content/path/to/file.mp3
```

Можна переглянути всі доступні голоси командою:

```bash
edge-tts --list-voices
```
