# Налаштування Store (Redux Toolkit)

Починаємо нову еру. З цього моменту ми будемо використовувати тільки **Redux Toolkit (RTK)**.

## Встановлення

Нам потрібно два пакети: сам тулкіт і зв'язка з React.
(Пакет `redux` встановлювати окремо не потрібно, він вже є всередині toolkit).

```bash
npm install @reduxjs/toolkit react-redux
```

## configureStore

Замість `createStore` ми використовуємо функцію `configureStore`.

::code-group
```javascript [store.js]
import { configureStore } from '@reduxjs/toolkit';
// Імпорти редюсерів (ми їх створимо пізніше)
// import counterReducer from './features/counter/counterSlice';

export const store = configureStore({
  reducer: {
    // counter: counterReducer
  },
});
```
::

### Що ми отримали безкоштовно?

Викликавши цю одну функцію, ми автоматично отримали:

1.  **Redux Thunk**: Middleware для асинхронності вже підключено.
2.  **Redux DevTools**: Розширення браузера підключено автоматично.
3.  **Immutability Check**: У режимі розробки (`process.env.NODE_ENV !== 'production'`) RTK перевірятиме, чи не мутували ви випадково стан. Якщо так — кине помилку в консоль.
4.  **Serializability Check**: Перевіряє, чи не поклали ви в стор щось заборонене (наприклад, Promise, Date object, Function). У сторі мають бути тільки прості JSON-дані.

### Підключення до React

Тут нічого не змінилося. Ми все ще використовуємо `<Provider>`.

```jsx [index.js]
import React from 'react';
import ReactDOM from 'react-dom/client';
import { Provider } from 'react-redux';
import { store } from './store';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <Provider store={store}>
    <App />
  </Provider>
);
```

Тепер нам потрібно створити логіку (редюсери та екшени). У RTK для цього є супер-інструмент: `createSlice`.

👉 [Далі: createSlice та магія Immer](./02.create-slice.md)
