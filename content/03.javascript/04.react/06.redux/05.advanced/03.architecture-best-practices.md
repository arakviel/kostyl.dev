# Найкращі практики та Архітектура

Ми пройшли довгий шлях від `switch` statements до RTK Query. На завершення давайте сформулюємо "Золоті правила" сучасної Redux-розробки.

## 1. Завжди використовуйте Redux Toolkit

Немає жодної причини писати "ванільний" Redux у новому проєкті. RTK — це стандарт.

## 2. Структура тек: Feature Folders

Замість того, щоб розділяти файли за типом (`/actions`, `/reducers`), групуйте їх за фічами (Features).

**Погано (Rails-style):**
```
src/
  actions/
    authActions.js
    todosActions.js
  reducers/
    authReducer.js
    todosReducer.js
```

**Добре (Feature-based):**
```
src/
  features/
    auth/
      authSlice.js
      LoginComponent.jsx
    todos/
      todosSlice.js
      TodoList.jsx
```
Це робить код модульним. Ви можете взяти папку `auth` і перенести в інший проєкт.

## 3. Не кладіть у Redux все підряд

Це найчастіша помилка.
-   **Form State**: Значення інпутів під час вводу. Використовуйте локальний `useState` або бібліотеки типу `React Hook Form`. У Redux кладіть дані тільки після Submit.
-   **UI State**: Відкриття дропдаунів, активні таби. Якщо це потрібно лише одному компоненту — залиште це в компоненті.

**Критерій "Чи потрібно це в Redux?":**
1.  Чи використовують ці дані інші частини програми?
2.  Чи потрібно відновлювати цей стан після перезавантаження сторінки?
3.  Чи хочете ви бачити історію змін цих даних у Time Travel Debugger?

## 4. Нормалізуйте складні дані

Якщо у вас є масиви об'єктів з ID — використовуйте `createEntityAdapter`. Це спростить оновлення та пошук, а також вбереже від дублікатів.

## 5. Використовуйте Селектори

Ніколи не пишіть `useSelector(state => state.users.items.find(...))` прямо в компоненті. Винесіть це у функцію-селектор.
Це дозволить вам змінювати структуру стейту без переписування всіх компонентів.

## 6. Логіка — в Reducers (або Thunks)

Намагайтеся тримати компоненти "дурними". Компонент повинен тільки відображати дані і діспатчити екшени.
-   **Погано**: Компонент обчислює складну знижку і відправляє результат в стор.
-   **Добре**: Компонент відправляє action `applyCoupon`, а редюсер (або thunk) рахує нову ціну.

## Висновок

Redux — це потужний інструмент. Він додає певний рівень складності (indirection), але натомість дає **передбачуваність** та **масштабованість**.

Якщо ви робите простий Todo-list — можливо, вам вистачить `Zustand` або `Context API`. Але якщо ви будуєте складну систему з великою кількістю взаємозв'язків — Redux (Toolkit) залишається королем.

Успіхів у кодингу! 🚀
