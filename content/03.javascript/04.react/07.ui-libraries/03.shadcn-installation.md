# Установка та Налаштування shadcn/ui

Тепер, коли ви розумієте філософію shadcn/ui, час перейти від теорії до практики. У цій главі ми встановимо shadcn/ui, налаштуємо проєкт та додамо перші компоненти.

**Що ми зробимо**:

- Встановимо shadcn/ui у Next.js проєкт
- Альтернативно: встановимо у Vite + React проєкт
- Розберемо кожен крок установки
- Налаштуємо `components.json`
- Додамо перший компонент
- Навчимося troubleshooting типових проблем

::note
**Важливо**: shadcn/ui — це не пакунок з NPM. Ми будемо копіювати компоненти у ваш проєкт. Тому **prerequisites** критичні — без них нічого не запрацює.
::

## Prerequisites (Що треба знати заздалегідь)

Перед початком переконайтеся, що ви розумієте ці технології:

### 1. React Fundamentals

- Components, Props, State
- Hooks (useState, useRef, useEffect)
- Event handling

Якщо ці терміни вам незн айомі, спочатку вивчіть базовий React.

### 2. TypeScript (Рекомендовано)

shadcn/ui написаний на TypeScript. Хоча можна використовувати з JavaScript, ви втаратите:

- Type safety
- Автокомпліт у VSCode
- Безпеку рефакторингу

### 3. Tailwind CSS

shadcn/ui heavily використовує Tailwind. Ви маєте розуміти:

- Utility classes (`flex`, `bg-primary`, `hover:`)
- Responsive prefixes (`sm:`, `md:`, `lg:`)
- Базову концепцію

Якщо ви не знайомі з Tailwind, рекомендую спочатку пройти їхній [tutorial](https://tailwindcss.com/docs/utility-first).

### 4. Node.js та Package Manager

- Node.js v18+ встановлений
- npm, pnpm або yarn

Перевірте версію:

```bash
node -v  # v18.0.0 або вище
npm -v   # 9.0.0 або вище
```

## Варіант 1: Установка з Next.js (Рекомендовано)

shadcn/ui ідеально інтегрується з **Next.js** (особливо App Router). Почнемо з нього.

::steps

### Крок 1: Створення Next.js Проєкту

Створіть новий Next.js проєкт:

```bash
npx create-next-app@latest my-app
```

CLI задасть кілька питань. Ось рекомендовані відповіді:

```
✔ Would you like to use TypeScript? … Yes
✔ Would you like to use ESLint? … Yes
✔ Would you like to use Tailwind CSS? … Yes
✔ Would you like your code inside a `src/` directory? … Yes
✔ Would you like to use App Router? … Yes
✔ Would you like to use Turbopack for `next dev`? … No
✔ Would you like to customize the import alias (@/* by default)? … No
```

**Чому саме такі відповіді?**

- **TypeScript**: Рекомендовано для type safety
- **Tailwind CSS**: Обов'язково (shadcn/ui використовує його)
- **src/ directory**: Організація проєкту (не критично)
- **App Router**: Сучасний Next.js підхід (Radix працює з обома)

Перейдіть у директорію:

```bash
cd my-app
```

### Крок 2: Ініціалізація shadcn/ui

Запустіть init команду:

```bash
npx shadcn@latest init
```

CLI задасть питання про конфігурацію:

```
✔ Preflight check
✔ Verifying framework. Found Next.js.
✔ Validating Tailwind CSS.
✔ Validating import alias.

✔ Which style would you like to use? › New York
✔ Which color would you like to use as the base color? › Zinc
✔ Would you like to use CSS variables for theming? › yes
```

**Розбір кожного питання**:

**1. Which style would you like to use?**

- **Default**: Більш rounded, м'який дизайн
- **New York**: Більш sharp, modern дизайн (рекомендую)

Це впливає на:

- Border radius компонентів
- Spacing та Typography
- Загальний "vibe"

Приклад різниці:

```tsx
// Default style
<Button className="rounded-md" />

// New York style
<Button className="rounded-sm" />
```

Це можна змінити пізніше, редагуючи компоненти.

**2. Which color would you like to use as base color?**

- Slate, Gray, Zinc, Neutral, Stone

Це базовий колір для neutral elements (borders, backgrounds, text).

Вибір Zinc:

```css
--background: 240 10% 3.9%;
--foreground: 0 0% 98%;
```

Вибір Slate:

```css
--background: 222.2 84% 4.9%;
--foreground: 210 40% 98%;
```

Різниця мінімальна (відтінок сірого). Zinc — універсальний вибір.

**3. Would you like to use CSS variables for theming?**

- **Yes**: Рекомендовано (легко міняти тему)
- **No**: Hardcoded Tailwind classes

З CSS variables:

```tsx
className = 'bg-primary' // → hsl(var(--primary))
```

Без CSS variables:

```tsx
className = 'bg-zinc-900' // → фіксований колір
```

**Завжди вибирайте Yes** — це дає гнучкість для dark mode та кастомних тем.

### Крок 3: Що відбулося під капотом?

Команда `shadcn init` створила/змінила кілька файлів:

::code-tree

```
my-app/
├── components.json       # ← Створено
├── src/
│   ├── app/
│   │   └── globals.css   # ← Оновлено
│   ├── components/
│   │   └── ui/           # ← Створено (порожня папка)
│   └── lib/
│       └── utils.ts      # ← Створено
└── tailwind.config.ts    # ← Оновлено
```

::

**Розберемо кожен файл**:

#### `components.json` (Конфігураційний файл)

```json
{
    "$schema": "https://ui.shadcn.com/schema.json",
    "style": "new-york",
    "rsc": true,
    "tsx": true,
    "tailwind": {
        "config": "tailwind.config.ts",
        "css": "src/app/globals.css",
        "baseColor": "zinc",
        "cssVariables": true,
        "prefix": ""
    },
    "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
    },
    "iconLibrary": "lucide"
}
```

**Поля конфігу**:

::field-group
::field{name="style" type="'default' | 'new-york'"}
Візуальний стиль компонентів.
::

::field{name="rsc" type="boolean"}
React Server Components підтримка (Next.js App Router).
::

::field{name="tsx" type="boolean"}
Чи генерувати `.tsx` файли (TypeScript).
::

::field{name="tailwind.config" type="string"}
Шлях до `tailwind.config.ts`.
::

::field{name="tailwind.css" type="string"}
Шлях до глобального CSS файлу.
::

::field{name="tailwind.baseColor" type="'slate' | 'gray' | 'zinc' | 'neutral' | 'stone'"}
Базовий колір для neutral елементів.
::

::field{name="tailwind.cssVariables" type="boolean"}
Використовувати CSS variables для кольорів.
::

::field{name="tailwind.prefix" type="string"}
Prefix для Tailwind класів (наприклад, `tw-`). Зазвичай порожній.
::

::field{name="aliases" type="object"}
Path aliases для import statements. **Критично важливе** — CLI використовує це для генерації imports.
::

::field{name="iconLibrary" type="'lucide' | 'custom'"}
Бібліотека іконок. Lucide — дефолт (React іконки від творців Feather Icons).
::
::

#### `src/lib/utils.ts` (Utility функція)

```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs))
}
```

**Що робить `cn()` функція?**

**Проблема**: Tailwind класи можуть конфліктувати:

```tsx
// Який колір буде?
<div className="bg-red-500 bg-blue-500" />
// Відповідь: залежить від порядку в згенерованому CSS (непередбачувано!)
```

**Рішення**: `twMerge` вирішує конфлікти:

```tsx
cn('bg-red-500', 'bg-blue-500')
// Результат: "bg-blue-500" (останній виграє)
```

**А `clsx`?** Зручний синтаксис для conditional classes:

```tsx
cn('button', isActive && 'active', isPrimary ? 'primary' : 'secondary', className)
```

**Використання в компонентах**:

```tsx
<Button className={cn('base-classes', variant === 'default' && 'default-classes', className)} />
```

::warning
**Важливо**: Всі shadcn/ui компоненти використовують `cn()`. Не видаляйте цей файл!
::

#### `src/app/globals.css` (Стилі теми)

CLI додав у ваш `globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
    :root {
        --background: 0 0% 100%;
        --foreground: 240 10% 3.9%;
        --card: 0 0% 100%;
        --card-foreground: 240 10% 3.9%;
        --popover: 0 0% 100%;
        --popover-foreground: 240 10% 3.9%;
        --primary: 240 5.9% 10%;
        --primary-foreground: 0 0% 98%;
        --secondary: 240 4.8% 95.9%;
        --secondary-foreground: 240 5.9% 10%;
        --muted: 240 4.8% 95.9%;
        --muted-foreground: 240 3.8% 46.1%;
        --accent: 240 4.8% 95.9%;
        --accent-foreground: 240 5.9% 10%;
        --destructive: 0 84.2% 60.2%;
        --destructive-foreground: 0 0% 98%;
        --border: 240 5.9% 90%;
        --input: 240 5.9% 90%;
        --ring: 240 5.9% 10%;
        --radius: 0.5rem;
    }

    .dark {
        --background: 240 10% 3.9%;
        --foreground: 0 0% 98%;
        --card: 240 10% 3.9%;
        --card-foreground: 0 0% 98%;
        /* ... dark mode кольори */
    }
}
```

**Чому HSL формат без `hsl()`?**

Це дозволяє додавати alpha (прозорість):

```tsx
className = 'bg-primary/50'
// → hsl(240 5.9% 10% / 0.5)
```

**Як змінити тему?**

Просто відредагуйте CSS variables:

```css
:root {
    --primary: 280 100% 50%; /* Фіолетовий замість чорного */
}
```

#### `tailwind.config.ts` (Tailwind конфігурація)

CLI оновив ваш Tailwind config:

```ts
import type { Config } from 'tailwindcss'

const config = {
    darkMode: ['class'],
    content: ['./pages/**/*.{ts,tsx}', './components/**/*.{ts,tsx}', './app/**/*.{ts,tsx}', './src/**/*.{ts,tsx}'],
    theme: {
        extend: {
            colors: {
                border: 'hsl(var(--border))',
                input: 'hsl(var(--input))',
                ring: 'hsl(var(--ring))',
                background: 'hsl(var(--background))',
                foreground: 'hsl(var(--foreground))',
                primary: {
                    DEFAULT: 'hsl(var(--primary))',
                    foreground: 'hsl(var(--primary-foreground))',
                },
                // ... інші кольори
            },
            borderRadius: {
                lg: 'var(--radius)',
                md: 'calc(var(--radius) - 2px)',
                sm: 'calc(var(--radius) - 4px)',
            },
        },
    },
    plugins: [require('tailwindcss-animate')],
} satisfies Config

export default config
```

**Ключові зміни**:

1. `darkMode: ["class"]` — dark mode через клас на `<html>`
2. `colors` прив'язані до CSS variables
3. `borderRadius` динамічні (можна змінити `--radius`)
4. Плагін `tailwindcss-animate` для анімацій

### Крок 4: Додавання Першого Компонента

Тепер проєкт налаштований. Додаймо кнопку:

```bash
npx shadcn@latest add button
```

**Що відбувається**:

1. CLI скачує код компонента з registry
2. Створює `src/components/ui/button.tsx`
3. Автоматично встановлює залежності (якщо потрібні)

**Результат**:

```
✔ Installing dependencies.
✔ Created 1 file:
  - src/components/ui/button.tsx
```

### Крок 5: Використання Компонента

Відкрийте `src/app/page.tsx`:

```tsx
import { Button } from '@/components/ui/button'

export default function Home() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center p-24">
            <h1 className="text-4xl font-bold mb-8">Welcome to shadcn/ui</h1>

            <div className="flex gap-4">
                <Button>Default</Button>
                <Button variant="secondary">Secondary</Button>
                <Button variant="destructive">Destructive</Button>
                <Button variant="outline">Outline</Button>
                <Button variant="ghost">Ghost</Button>
            </div>
        </main>
    )
}
```

### Крок 6: Запуск Dev Server

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000).

Ви маєте побачити 5 кнопок з різними варіантами! 🎉

::

## Варіант 2: Установка з Vite + React

Якщо ви не використовуєте Next.js, ось як встановити з Vite.

::steps

### Крок 1: Створення Vite Проєкту

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app
npm install
```

### Крок 2: Установка Tailwind CSS

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

Оновіть `tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
export default {
    darkMode: ['class'],
    content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
    theme: {
        extend: {},
    },
    plugins: [],
}
```

Створіть `src/index.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Імпортуйте у `src/main.tsx`:

```tsx
import './index.css'
```

### Крок 3: Налаштування Path Aliases

Vite не підтримує `@/*` aliases з коробки. Потрібна конфігурація.

**1. Встановіть types для node**:

```bash
npm install -D @types/node
```

**2. Оновіть `vite.config.ts`**:

```ts
import path from 'path'
import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

export default defineConfig({
    plugins: [react()],
    resolve: {
        alias: {
            '@': path.resolve(__dirname, './src'),
        },
    },
})
```

**3. Оновіть `tsconfig.json`**:

```json
{
    "compilerOptions": {
        // ... інші опції
        "baseUrl": ".",
        "paths": {
            "@/*": ["./src/*"]
        }
    }
}
```

**4. Оновіть `tsconfig.app.json`** (якщо є):

```json
{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": ["./src/*"]
        }
    }
}
```

### Крок 4: Ініціалізація shadcn/ui

```bash
npx shadcn@latest init
```

Відповіді:

```
✔ Preflight check.
✔ Verifying framework. Found Vite.
✔ Validating Tailwind CSS.

✔ Which style would you like to use? › New York
✔ Which color would you like to use as the base color? › Zinc
✔ Would you like to use CSS variables for theming? › yes
```

::warning
**Важливо**: CLI може не знайти `globals.css` автоматично для Vite. Вкажіть вручну:

```
✔ Where is your global CSS file? › src/index.css
```

::

### Крок 5: Додавання Компонентів

Аналогічно Next.js:

```bash
npx shadcn@latest add button
```

### Крок 6: Використання

У `src/App.tsx`:

```tsx
import { Button } from '@/components/ui/button'

function App() {
    return (
        <div className="flex min-h-screen items-center justify-center">
            <Button>Click me</Button>
        </div>
    )
}

export default App
```

::

## Структура Проєкту після Установки

Після setup ваш проєкт має таку структуру:

::code-tree

```
my-app/
├── components.json
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── src/
│   ├── app/ (Next.js) або main.tsx (Vite)
│   ├── components/
│   │   └── ui/
│   │       └── button.tsx  # ← Доданий компонент
│   └── lib/
│       └── utils.ts        # ← cn() функція
└── node_modules/
```

::

**Пояснення**:

- `components.json`: Конфігурація shadcn/ui
- `src/components/ui/`: Тут будуть ВСІ компоненти shadcn/ui
- `src/lib/utils.ts`: Utility функції (основна — `cn()`)

::note
**Важливо**: Компоненти у `src/components/ui/` — це **ваш код**. Ви можете редагувати їх як завгодно. Це не `node_modules`.
::

## Додавання Кількох Компонентів Одразу

Можна додати кілька компонентів однією командою:

```bash
npx shadcn@latest add button card dialog input label
```

Або додати все, що може знадобитися (⚠️ багато файлів):

```bash
npx shadcn@latest add
# CLI покаже список всіх доступних компонентів
# Оберіть потрібні (Space для вибору, Enter для підтвердження)
```

## Огляд Компонента: Button

Подивімось на згенерований код кнопки:

```tsx
// src/components/ui/button.tsx
import * as React from 'react'
import { Slot } from '@radix-ui/react-slot'
import { cva, type VariantProps } from 'class-variance-authority'
import { cn } from '@/lib/utils'

const buttonVariants = cva(
    'inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0',
    {
        variants: {
            variant: {
                default: 'bg-primary text-primary-foreground shadow hover:bg-primary/90',
                destructive: 'bg-destructive text-destructive-foreground shadow-sm hover:bg-destructive/90',
                outline: 'border border-input bg-background shadow-sm hover:bg-accent hover:text-accent-foreground',
                secondary: 'bg-secondary text-secondary-foreground shadow-sm hover:bg-secondary/80',
                ghost: 'hover:bg-accent hover:text-accent-foreground',
                link: 'text-primary underline-offset-4 hover:underline',
            },
            size: {
                default: 'h-9 px-4 py-2',
                sm: 'h-8 rounded-md px-3 text-xs',
                lg: 'h-10 rounded-md px-8',
                icon: 'h-9 w-9',
            },
        },
        defaultVariants: {
            variant: 'default',
            size: 'default',
        },
    },
)

export interface ButtonProps
    extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> {
    asChild?: boolean
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
    ({ className, variant, size, asChild = false, ...props }, ref) => {
        const Comp = asChild ? Slot : 'button'
        return <Comp className={cn(buttonVariants({ variant, size, className }))} ref={ref} {...props} />
    },
)
Button.displayName = 'Button'

export { Button, buttonVariants }
```

**Розбір коду**:

**1. CVA для variants**:

```tsx
const buttonVariants = cva(/* базові класи */, {
  variants: { /* варіації */ },
  defaultVariants: { /* дефолти */ }
})
```

**2. TypeScript типи**:

```tsx
export interface ButtonProps
    extends
        React.ButtonHTMLAttributes<HTMLButtonElement>, // Усі HTML button props
        VariantProps<typeof buttonVariants> {
    // variant та size props
    asChild?: boolean // Radix Slot API
}
```

**3. Radix Slot** (опціонально):

```tsx
<Button asChild>
    <a href="/home">Home</a>
</Button>
// Рендерить <a> з стилями кнопки, а не <button>
```

**4. forwardRef** для ref передачі:

```tsx
const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(...)
// Можна використати ref:
const buttonRef = useRef<HTMLButtonElement>(null)
<Button ref={buttonRef} />
```

## Кастомізація Компонента

Тепер найцікавіше: **як змінювати компоненти?**

### Приклад 1: Додати новий variant

Відкрийте `button.tsx` та додайте:

```tsx {9}
const buttonVariants = cva('...', {
    variants: {
        variant: {
            default: '...',
            destructive: '...',
            // ↓ Додаємо новий variant
            success: 'bg-green-600 text-white shadow-sm hover:bg-green-700',
        },
    },
})
```

Використання:

```tsx
<Button variant="success">Save</Button>
```

TypeScript автоматично додасть `'success'` до автокоміта!

### Приклад 2: Змінити базовий стиль

Хочете rounded-lg замість rounded-md?

```tsx {2}
const buttonVariants = cva(
    'inline-flex items-center justify-center rounded-lg ...', // ← змінили md на lg
    {
        /* ... */
    },
)
```

Усі кнопки тепер мають більший border-radius.

### Приклад 3: Додати іконку

Створіть wrapper компонент:

```tsx
// src/components/icon-button.tsx
import { Button, ButtonProps } from '@/components/ui/button'
import { Loader2 } from 'lucide-react'

interface IconButtonProps extends ButtonProps {
    icon?: React.ReactNode
    loading?: boolean
}

export function IconButton({ icon, loading, children, ...props }: IconButtonProps) {
    return (
        <Button disabled={loading || props.disabled} {...props}>
            {loading ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : icon && <span className="mr-2">{icon}</span>}
            {children}
        </Button>
    )
}
```

Використання:

```tsx
import { Save } from 'lucide-react'
;<IconButton icon={<Save />} loading={isSubmitting}>
    Save Changes
</IconButton>
```

## Troubleshooting: Типові Проблеми

### Проблема 1: "Cannot find module '@/components/ui/button'"

**Причина**: Path aliases не налаштовані.

**Рішення** для Vite:

```ts
// vite.config.ts
import path from "path"

export default define Config({
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
})
```

```json
// tsconfig.json
{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": ["./src/*"]
        }
    }
}
```

### Проблема 2: Tailwind класи не застосовуються

**Причина**: Tailwind не сканує `src/components/ui/`.

**Рішення**:

```js
// tailwind.config.js
module.exports = {
    content: [
        './src/**/*.{js,jsx,ts,tsx}', // ← Включає ВСІ файли в src/
    ],
}
```

### Проблема 3: CSS variables не працюють

**Причина**: Не імпортовано `globals.css`.

**Рішення** для Next.js:

```tsx
// src/app/layout.tsx
import './globals.css' // ← Переконайтесь, що це є
```

**Рішення** для Vite:

```tsx
// src/main.tsx
import './index.css' // ← Тут мають бути @tailwind directives
```

### Проблема 4: Dark mode не працює

**Причина**: Клас `.dark` не додається на `<html>`.

**Рішення** для Next.js (використайте `next-themes`):

```bash
npm install next-themes
```

```tsx
// src/app/layout.tsx
import { ThemeProvider } from 'next-themes'

export default function RootLayout({ children }: { children: React.ReactNode }) {
    return (
        <html lang="en" suppressHydrationWarning>
            <body>
                <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
                    {children}
                </ThemeProvider>
            </body>
        </html>
    )
}
```

Theme toggle компонент:

```tsx
'use client'

import { useTheme } from 'next-themes'
import { Button } from '@/components/ui/button'

export function ThemeToggle() {
    const { setTheme, theme } = useTheme()

    return (
        <Button variant="outline" onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}>
            Toggle theme
        </Button>
    )
}
```

## Підсумок: Checklist Успішної Установки

- [ ] Node.js v18+ встановлений
- [ ] Tailwind CSS налаштований
- [ ] Path aliases працюють (`@/components` та `@/lib`)
- [ ] `components.json` створений
- [ ] `src/lib/utils.ts` існує з `cn()` функцією
- [ ] `globals.css` містить CSS variables
- [ ] Перший компонент (Button) доданий та працює
- [ ] Dark mode налаштований (опціонально)

Якщо всі пункти виконані — ви готові до використання shadcn/ui! 🎉

У наступній главі ми детально розберемо базові компоненти: Button, Card, Badge, Avatar та інші фундаментальні елементи інтерфейсу.

[Далі: Базові Компоненти →](./04.shadcn-components-basics.md)
