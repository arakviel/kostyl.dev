---
title: 'Створення кастомних Guards'
description: 'Імплементація CanActivate, логіка авторизації'
---

# Створення кастомних Guards

## Короткий зміст

- Створення класу з інтерфейсом CanActivate
- Декоратор @Injectable() для guards
- Метод canActivate(context): логіка перевірки доступу
- Витягування request з ExecutionContext
- Перевірка токена, ролей, дозволів
- Приклад: AuthGuard для перевірки JWT токена
- Приклад: RolesGuard для перевірки ролей користувача
- Використання Reflector для читання метаданих
- Комбінування кількох guards
- Best practices: розділення автентифікації та авторизації
