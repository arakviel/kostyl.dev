path = "/Users/arakviel/.gemini/antigravity-ide/brain/45e89097-76d3-4baa-b59c-76aeaabc5cb9/walkthrough.md"
with open(path, "r", encoding="utf-8") as f:
    content = f.read().rstrip()

addition = """

---

## 13. Модуль 10: Градієнтний бустінг та XGBoost (`10.gradient-boosting`)

### 13.1. Генерація графіків Matplotlib (300 DPI)
Створено скрипт `scripts/generate_boosting_images.py` та згенеровано 2 діаграми:
- `01.png` (`public/images/ai-python/gradient-boosting/gradient-boosting-xgboost/01.png`):
  Стовпчикова діаграма систематичного зміщення Random Forest за 6 ціновими діапазонами California Housing ($0–150k до $450k+) із відображенням величини недооцінки дорогих об'єктів.
- `02.png` (`public/images/ai-python/gradient-boosting/gradient-boosting-xgboost/02.png`):
  Групована горизонтальна діаграма важливості ознак (Feature Importance) для порівняння Random Forest vs XGBoost з двомовними підписами ознак (`MedInc (медіанний дохід жителів, $10k)` тощо).

### 13.2. Оновлення матеріалів лекції
В [01.gradient-boosting-xgboost.md](file:///Users/arakviel/Work/kostyl.dev/content/16.ai-python/10.gradient-boosting/01.gradient-boosting-xgboost.md):
- **Усі 7 блоків `#output`** замінено на естетичні Markdown-таблиці та картки:
  1. Таблиця покрокового навчання трьох дерев бустінгу на залишках помилок із відстеженням падіння сумарної помилки.
  2. KPI-картка базового Random Forest ($R^2=0.8093$, RMSE $= 0.501$).
  3. Вбудовано діаграму систематичного зміщення `01.png` (`{.diagram-img}`) та аналітичну таблицю похибок за ціновими сегментами.
  4. Діагностична картка процесу навчання XGBoost (100 дерев, `learning_rate=0.1`).
  5. Порівняльна таблиця метрик Random Forest vs XGBoost ($R^2=0.8351$, перевага XGBoost у швидкості та точності).
  6. Вбудовано діаграму важливості ознак `02.png` (`{.diagram-img}`) та структуровану таблицю ранжування ознак.
  7. Картка результатів 5-fold крос-валідації ($R^2 = 0.8340 \\pm 0.0062$).
- Виправлено інлайн-формулу на рядку 412: змінено `::math-formula{... inline}` на правильний `:math-formula{... inline}`.

---

## 14. Модуль 11: Логістична регресія та метрики класифікації (`11.logistic-regression-classification`)

### 14.1. Генерація графіків Matplotlib (300 DPI)
Створено скрипти `scripts/generate_classification_images.py` та згенеровано 11 високоякісних графіків:
- **`classification-basics/` (6 діаграм, `01.png` – `06.png`):**
  - `01.png`: Обмеження лінійної регресії в задачах бінарної класифікації (вихід за межі діапазону від 0 до 1, некоректні ймовірності).
  - `02.png`: Логістична функція активації (сигмоїда) з точкою симетрії $(0, 0.5)$, асимптотами, порогом рішень та зонами класифікації.
  - `03.png`: Двопанельний графік компонентів Binary Cross-Entropy Loss: круте покарання при $\\hat{y} \\to 0$ для $y=1$ та при $\\hat{y} \\to 1$ для $y=0$.
  - `04.png`: Порівняння функцій втрат BCE та MSE для класифікації (ілюстрація ефекту згасання градієнта в MSE проти швидкої збіжності BCE).
  - `05.png`: Динаміка збіжності градієнтного спуску для `LogisticRegressionScratch` протягом 1000 ітерацій.
  - `06.png`: Двовимірна лінійна межа рішення (Decision Boundary) для класифікації ірисів Фішера (Setosa vs Versicolor) з точністю 100%.
- **`classification-metrics/` (5 діаграм, `01.png` – `05.png`):**
  - `01.png`: Теплова карта Confusion Matrix ($2 \\times 2$) з чіткими числовими підписами та розшифровкою категорій TP, FP, FN, TN.
  - `02.png`: Графік компромісу між точністю та повнотою (Precision vs Recall Trade-off) залежно від порогу класифікації з точкою рівноваги.
  - `03.png`: ROC-крива логістичної регресії з підрахунком AUC ($0.998$) та лінією випадкового класифікатора ($0.500$).
  - `04.png`: Матриця помилок для реальної медичної задачі діагностики раку молочної залози (Breast Cancer, $114$ пацієнтів: TN=41, FP=2, FN=2, TP=69).
  - `05.png`: ROC-крива на тестовій вибірці Breast Cancer з відміткою AUC ($0.993$).

### 14.2. Оновлення файлів матеріалів

1. **[01.classification-basics.md](file:///Users/arakviel/Work/kostyl.dev/content/16.ai-python/11.logistic-regression-classification/01.classification-basics.md):**
   - **Усі 11 блоків `#output`** оновлено до стандартів курсу:
     1. Вбудовано діаграму нелінійності `01.png` (`{.diagram-img}`).
     2. Таблиця передбачень лінійної моделі для годин навчання ($x \\in [0, 5, 10, 12]$) з підсвічуванням фізично неможливих значень ймовірності.
     3. Вбудовано діаграму сигмоїди `02.png` (`{.diagram-img}`).
     4. Таблиця розрахункових значень $\\sigma(z)$ для $z \\in [-10, \\dots, 10]$ із практичною інтерпретацією.
     5. Вбудовано графік компонентів втрат BCE `03.png` (`{.diagram-img}`).
     6. Вбудовано порівняльний графік BCE vs MSE `04.png` (`{.diagram-img}`).
     7. Інформаційний алерт успішної компіляції класу `LogisticRegressionScratch`.
     8. KPI-картка тесту (Accuracy 95.00%, фінальний Loss 0.1742) та вбудована діаграма збіжності `05.png` (`{.diagram-img}`).
     9. Картка параметрів вибірки Breast Cancer (569 зразків, 30 ознак, баланс класів, 96.49% точність).
     10. Таблиця передбачених моделлю ймовірностей $P(y=0)$ та $P(y=1)$ для перших 5 зразків.
     11. Алерт бездоганної класифікації (100%) та вбудована діаграма `06.png` (`{.diagram-img}`).
   - Додано україномовні ботанічні еквіваленти для ознак у дужках (`sepal length (довжина чашолистка)` тощо).

2. **[02.classification-metrics.md](file:///Users/arakviel/Work/kostyl.dev/content/16.ai-python/11.logistic-regression-classification/02.classification-metrics.md):**
   - Усунуто 425 рядків продубльованого конспекту-чернетки на початку документу, відновлено чисту ієрархію заголовків.
   - **Усі 13 блоків `#output`** переведено у структуровані компоненти:
     1. Таблиця зведеної матриці помилок та вбудована діаграма `01.png` (`{.diagram-img}`).
     2. Картка Precision ($90.00\\%$) з прикладом поштового спам-фільтра.
     3. Картка Recall ($90.00\\%$) з акцентом на ризиках пропуску хворих пацієнтів (FN).
     4. Вбудовано графік динаміки метрик за порогом `02.png` (`{.diagram-img}`).
     5. Таблиця поведінки класифікатора для трьох стратегічних порогів ($T \\in [0.3, 0.5, 0.7]$).
     6. Порівняльна таблиця середнього арифметичного та F1-score з демонстрацією штрафування сильного дисбалансу.
     7. Повна таблиця класифікаційного звіту (`classification_report`) з класами «Злоякісна» та «Доброякісна».
     8. Вбудовано теоретичну ROC-криву `03.png` (`{.diagram-img}`).
     9. Зведена картка вибірки та метрик Breast Cancer (Accuracy 0.965, Precision 0.958, Recall 0.972, F1 0.965, ROC-AUC 0.993).
     10. Таблиця верифікації матриці помилок Breast Cancer та вбудована діаграма `04.png` (`{.diagram-img}`).
     11. Вбудовано діаграму клінічної ROC-кривої `05.png` (`{.diagram-img}`).
     12. Картки та таблиця топ-5 оптимальних порогів класифікації (максимум $F_1 = 0.972$ при $T=0.45$).
     13. Зведена порівняльна таблиця трьох алгоритмів (Logistic Regression vs Decision Tree vs Random Forest).
   - Виправлено перенос рядків у резюме рекомендацій для медицини.

### 14.3. Результати верифікації
- **Зображення:** Усі 11 діаграм згенеровані у роздільній здатності 300 DPI, перевірені на диску та коректно розміщені з тегом `{.diagram-img}`.
- **MDC-синтаксис:** Автоматична стекова верифікація контейнерів підтвердила **0 синтаксичних помилок** в обох файлах.
- **Вивід:** 100% блоків `#output` переведені у валідний Markdown (таблиці, KPI-картки, алерти), жодних сирих ASCII-блоків чи порожніх плейсхолдерів.
"""

with open(path, "w", encoding="utf-8") as f:
    f.write(content + addition + "\n")

print("walkthrough.md successfully updated!")
