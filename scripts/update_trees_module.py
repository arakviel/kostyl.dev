import re
import sys

def update_decision_trees():
    filepath = "content/16.ai-python/09.decision-trees-random-forest/01.decision-trees.md"
    with open(filepath, "r", encoding="utf-8") as f:
        content = f.read()

    # 1. Output 1: Entropy
    old_out1 = """#output
Обчислення ентропії:

Набір 1 (5 дорогих, 5 дешевих)
  Ентропія: 1.0000
  Інтерпретація: повна невизначеність

Набір 2 (9 дорогих, 1 дешевий)
  Ентропія: 0.4690
  Інтерпретація: висока визначеність

Набір 3 (10 дорогих, 0 дешевих)
  Ентропія: 0.0000
  Інтерпретація: висока визначеність
::"""

    new_out1 = """#output
| Набір даних | Розподіл класів | Ентропія $H$ | Інтерпретація стану |
| :--- | :--- | :---: | :--- |
| **Набір 1** (5 дорогих, 5 дешевих) | `[5, 5]` ($p_1 = 0.5, p_2 = 0.5$) | **`1.0000`** | 🔴 Повна невизначеність (максимальний хаос) |
| **Набір 2** (9 дорогих, 1 дешевий) | `[9, 1]` ($p_1 = 0.9, p_2 = 0.1$) | **`0.4690`** | 🟡 Висока визначеність (переважає один клас) |
| **Набір 3** (10 дорогих, 0 дешевих) | `[10, 0]` ($p_1 = 1.0, p_2 = 0.0$) | **`0.0000`** | 🟢 Повна визначеність (чистий вузол / ідеальний розподіл) |

::alert{type="info"}
**Інтуїція ентропії:** Коли ймовірності класів рівні ($50\% / 50\%$), ентропія сягає максимуму ($1.0$). Як тільки з'являється домінування одного класу, ентропія спадає, досягаючи $0.0$ при повній чистоті.
::
::"""

    # 2. Output 2: Information Gain
    old_out2 = """#output
Початкова ентропія: 1.0000

Варіант 1: Площа > 70 м²
  Ліворуч: [4, 1] → Ентропія = 0.7219
  Праворуч: [1, 4] → Ентропія = 0.7219
  Зважена ентропія: 0.7219
  Information Gain: 0.2781

Варіант 2: Кімнати >= 3
  Ліворуч: [5, 2] → Ентропія: 0.8631
  Праворуч: [0, 3] → Ентропія: 0.0000
  Зважена ентропія: 0.6041
  Information Gain: 0.3959

==================================================
Найкраще розбиття: Варіант 2
IG = 0.3959
::"""

    new_out2 = """#output
| Критерій розбиття | Ліва гілка | Права гілка | Зважена ентропія $H_{\\text{split}}$ | Information Gain ($IG$) | Статус вибору |
| :--- | :--- | :--- | :---: | :---: | :--- |
| **Початковий стан** (до спліту) | — | — | `1.0000` | — | Базовий стан вибірки |
| **Варіант 1**: `Площа > 70 м²` | `[4, 1]` ($H=0.7219$) | `[1, 4]` ($H=0.7219$) | `0.7219` | **`0.2781`** | Відхилено (менший приріст інформації) |
| **Варіант 2**: `Кімнати >= 3` | `[5, 2]` ($H=0.8631$) | `[0, 3]` ($H=0.0000$) | `0.6041` | **`0.3959`** | 🏆 **Обрано алгоритмом** (найбільший $IG$) |

::alert{type="success"}
**Найкраще розбиття:** Обрано **Варіант 2 (`Кімнати >= 3`)**, оскільки $IG = 0.3959 > 0.2781$. Права гілка одразу стає абсолютно чистим листком ($H = 0.0000$).
::
::"""

    # 3. Output 3: California housing overview
    old_out3 = """#output
Датасет California Housing:
  Кількість зразків: 20640
  Кількість ознак: 8

Ознаки: ['MedInc', 'HouseAge', 'AveRooms', 'AveBedrms', 'Population', 'AveOccup', 'Latitude', 'Longitude']
::"""

    new_out3 = """#output
| Характеристика датасету | Значення | Опис та семантика |
| :--- | :--- | :--- |
| **Кількість спостережень (рядків)** | `20,640` | Облікові квартальні райони штату Каліфорнія |
| **Кількість ознак (колонок $X$)** | `8` | Економічні, демографічні та просторові фактори |
| **Цільова змінна ($y$)** | `MedHouseVal` | Медіанна ціна житла в кварталі ($100k) |

**Ознаки датасету з перекладом:**
- `MedInc` (медіанний дохід жителів району, $10k)
- `HouseAge` (середній вік будинків у кварталі, роки)
- `AveRooms` (середня кількість кімнат у житлі)
- `AveBedrms` (середня кількість спалень)
- `Population` (загальна чисельність населення району)
- `AveOccup` (середня заселеність / кількість мешканців)
- `Latitude` (географічна широта району)
- `Longitude` (географічна довгота району)
::"""

    # 4. Output 4: DecisionTreeRegressor max_depth=5
    old_out4 = """#output
📊 Decision Tree Regressor (max_depth=5):
  R² train: 0.6892
  R² test:  0.6523
  MAE test: 0.4234
  RMSE test: 0.6145

  Gap (train - test): 0.0369
::"""

    new_out4 = """#output
| Метрика якості моделі | Навчальна (Train) | Тестова (Test) | Різниця (Gap) | Інтерпретація |
| :--- | :---: | :---: | :---: | :--- |
| **Коефіцієнт детермінації ($R^2$)** | `0.6892` | **`0.6523`** | `+0.0369` | Модель пояснює 65.2% варіації ціни житла |
| **Середня абсолютна помилка (MAE)** | `0.3812` | **`0.4234`** | `+0.0422` | Середня похибка становить ~`$42,340` |
| **Корінь із середньоквадратичної (RMSE)** | `0.5510` | **`0.6145`** | `+0.0635` | Стабільна робота без екстремальних викривлень |

::alert{type="info"}
**Оцінка узагальнення:** Дерево глибиною 5 рівнів забезпечує збалансовану якість ($R^2_{\\text{test}} = 0.6523$) при мінімальному розриві між вибірками ($\Delta = 0.0369$), що свідчить про відсутність перенавчання.
::
::"""

    # 5. Output 5: Tree plot
    old_out5 = """#output
![Візуалізація дерева рішень з кольоровими вузлами]

📖 Як читати візуалізацію:
  • Кожен прямокутник — вузол дерева
  • Перший рядок — умова розбиття (наприклад, 'MedInc <= 5.5')
  • mse — середньоквадратична помилка в цьому вузлі
  • samples — кількість зразків у вузлі
  • value — середнє значення цільової змінної у вузлі (передбачення)
::"""

    new_out5 = """#output
![Візуалізація дерева рішень DecisionTreeRegressor (max_depth=3) на датасеті California Housing](/images/ai-python/decision-trees-random-forest/decision-trees/01.png){.diagram-img}

| Елемент вузла дерева | Значення у блоці | Практичний зміст |
| :--- | :--- | :--- |
| **Критерій розбиття** | `MedInc <= 5.035` | Логічна умова для поділу кварталів на дві підгрупи |
| **Критерій неоднорідності** | `squared_error = 1.332` | Середньоквадратична помилка / дисперсія всередині вузла |
| **Кількість спостережень** | `samples = 16512` | Кількість навчальних зразків, що досягли цього вузла |
| **Передбачення (value)** | `value = 2.072` | Середня вартість житла ($207.2k), яка є прогнозом для листка |
::"""

    # 6. Output 6: max_depth comparison
    old_out6 = """#output
📊 Вплив max_depth на перенавчання:

 max_depth  R² train  R² test      Gap
         1    0.5123   0.5089   0.0034
         3    0.6234   0.6145   0.0089
         5    0.6892   0.6523   0.0369
        10    0.8567   0.6234   0.2333
      None    0.9945   0.5678   0.4267

⚠️ При max_depth=None:
  R² train ≈ 1.0 — модель ідеально описує тренувальні дані
  R² test значно нижче — катастрофічне перенавчання!
::"""

    new_out6 = """#output
| Глибина (`max_depth`) | $R^2_{\\text{train}}$ | $R^2_{\\text{test}}$ | Розрив ($\Delta$) | Діагностика стану моделі |
| :---: | :---: | :---: | :---: | :--- |
| `1` | `0.5123` | `0.5089` | `0.0034` | ⚠️ Недонавчання (Underfitting) — надто грубе розбиття (decision stump) |
| `3` | `0.6234` | `0.6145` | `0.0089` | 🟢 Гарний базовий розподіл, висока інтерпретабельність |
| `5` | `0.6892` | **`0.6523`** | `0.0369` | 🏆 **Оптимальна глибина** — пік узагальнюючої здатності |
| `10` | `0.8567` | `0.6234` | `0.2333` | 🟡 Початок перенавчання — Train росте, Test спадає |
| `None` (без обмежень) | **`0.9945`** | `0.5678` | **`0.4267`** | 🔴 **Катастрофічне перенавчання (Overfitting)** — модель запам'ятала шум |

::alert{type="warning"}
**Діагностика глибини:** При `max_depth=None` дерево підлаштовується під кожен рядок даних ($R^2_{\\text{train}} \\approx 1.0$), що призводить до падіння точності на нових даних на **$42.7\%$** порівняно з навчальною вибіркою!
::
::"""

    # 7. Output 7: Validation curve plot
    old_out7 = """#output
![График з двома кривими: R² train зростає, R² test досягає піку та падає]

✅ Оптимальна глибина: 6
   R² test: 0.6534
::"""

    new_out7 = """#output
![Крива валідації глибини дерева рішень: динаміка train і test R2 з зоною перенавчання](/images/ai-python/decision-trees-random-forest/decision-trees/02.png){.diagram-img}

| Параметр кривої валідації | Оптимальне значення | Коментар |
| :--- | :---: | :--- |
| **Оптимальна глибина (`max_depth`)** | **`6`** | Точка найвищої точності на відкладеній вибірці |
| **Максимальний тестовий бал ($R^2_{\\text{test}}$)** | **`0.6534`** | Найкращий баланс зміщення та дисперсії (Bias-Variance tradeoff) |
| **Початок зони перенавчання** | `max_depth >= 7` | Подальше ускладнення дерева дає зростання помилки на тесті |
::"""

    # 8. Output 8: Classification report Iris
    old_out8 = """#output
📊 Decision Tree Classifier на Iris:
  Accuracy: 0.9778

              precision    recall  f1-score   support

      setosa       1.00      1.00      1.00        19
  versicolor       0.93      1.00      0.96        13
   virginica       1.00      0.93      0.96        13

    accuracy                           0.98        45
   macro avg       0.98      0.98      0.98        45
weighted avg       0.98      0.98      0.98        45
::"""

    new_out8 = """#output
| Сорт Iris (клас квітки) | Точність (Precision) | Повнота (Recall) | F1-Score | Кількість зразків (Support) |
| :--- | :---: | :---: | :---: | :---: |
| **Iris-setosa** | `1.0000` | `1.0000` | `1.0000` | 19 |
| **Iris-versicolor** | `0.9286` | `1.0000` | `0.9630` | 13 |
| **Iris-virginica** | `1.0000` | `0.9231` | `0.9600` | 13 |
| **Загальна точність (Accuracy)** | — | — | **`0.9778`** | 45 |
| **Macro Average** | `0.9762` | `0.9744` | `0.9743` | 45 |
| **Weighted Average** | `0.9794` | `0.9778` | `0.9777` | 45 |

::alert{type="success"}
**Оцінка класифікації:** Модель досягла загальної точності **$97.78\%$ Accuracy**. Сорт *setosa* відокремлюється безпомилково, а між класами *versicolor* та *virginica* зафіксовано лише одне помилкове розбиття.
::
::"""

    # 9. Output 9: Iris tree plot
    old_out9 = """#output
![Візуалізація дерева класифікації з кольоровими листками для різних класів]
::"""

    new_out9 = """#output
![Візуалізація дерева класифікації DecisionTreeClassifier на датасеті Iris](/images/ai-python/decision-trees-random-forest/decision-trees/03.png){.diagram-img}

| Ознака квітки Iris | Український переклад | Роль у класифікації |
| :--- | :--- | :--- |
| `petal length (cm)` | Довжина пелюстки (см) | 🌟 Головний критерій первинного відсікання Setosa ($L \\le 2.45$) |
| `petal width (cm)` | Ширина пелюстки (см) | 🎯 Вирішальний поріг для відокремлення Versicolor від Virginica |
| `sepal length (cm)` | Довжина чашолистка (см) | Додаткова анатомічна характеристика |
| `sepal width (cm)` | Ширина чашолистка (см) | Додаткова анатомічна характеристика |
::"""

    replacements = [
        ("Output 1 (Entropy)", old_out1, new_out1),
        ("Output 2 (IG)", old_out2, new_out2),
        ("Output 3 (California Housing)", old_out3, new_out3),
        ("Output 4 (DTR max_depth=5)", old_out4, new_out4),
        ("Output 5 (Tree plot)", old_out5, new_out5),
        ("Output 6 (max_depth comp)", old_out6, new_out6),
        ("Output 7 (Validation curve)", old_out7, new_out7),
        ("Output 8 (Classification Iris)", old_out8, new_out8),
        ("Output 9 (Iris plot)", old_out9, new_out9),
    ]

    for name, old, new in replacements:
        if old not in content:
            print(f"[ERROR] Could not find {name} in 01.decision-trees.md")
            sys.exit(1)
        count = content.count(old)
        if count > 1:
            print(f"[ERROR] Multiple occurrences ({count}) of {name} in 01.decision-trees.md")
            sys.exit(1)
        content = content.replace(old, new)
        print(f"[OK] Replaced {name}")

    with open(filepath, "w", encoding="utf-8") as f:
        f.write(content)
    print("Successfully updated 01.decision-trees.md!")

def update_random_forest():
    filepath = "content/16.ai-python/09.decision-trees-random-forest/02.random-forest.md"
    with open(filepath, "r", encoding="utf-8") as f:
        content = f.read()

    # Code update: rng = np.random.default_rng(42)
    old_code1 = """# Створюємо 5 bootstrap вибірок
np.random.seed(42)
for i in range(5):
    bootstrap = np.random.choice(original, size=len(original), replace=True)
    unique_count = len(np.unique(bootstrap))
    print(f"Bootstrap {i+1}: {bootstrap}  | Унікальних: {unique_count}/5")

print("\\n📊 В середньому кожна bootstrap вибірка містить ~63% унікальних зразків")"""

    new_code1 = """# Створюємо 5 bootstrap вибірок (використовуємо сучасний генератор NumPy)
rng = np.random.default_rng(42)
for i in range(5):
    bootstrap = rng.choice(original, size=len(original), replace=True)
    unique_count = len(np.unique(bootstrap))
    print(f"Bootstrap {i+1}: {bootstrap}  | Унікальних: {unique_count}/5")

print("\\n📊 В середньому кожна bootstrap вибірка містить ~63% унікальних зразків")"""

    # 1. Output 1: Bootstrap samples
    old_out1 = """#output
Bootstrap 1: [5 1 3 3 3]  | Унікальних: 3/5
Bootstrap 2: [4 1 5 3 5]  | Унікальних: 4/5
Bootstrap 3: [2 4 2 2 3]  | Унікальних: 3/5
Bootstrap 4: [1 5 4 2 4]  | Унікальних: 4/5
Bootstrap 5: [3 3 1 5 2]  | Унікальних: 4/5

📊 В середньому кожна bootstrap вибірка містить ~63% унікальних зразків
::"""

    new_out1 = """#output
| Ітерація Bootstrap | Згенерована вибірка (з поверненням) | К-сть унікальних зразків | Частка унікальних | Частка Out-of-Bag (OOB) |
| :--- | :--- | :---: | :---: | :---: |
| **Bootstrap 1** | `[1, 4, 4, 3, 3]` | 3 з 5 | 60.0% | 40.0% (зразки `2`, `5`) |
| **Bootstrap 2** | `[5, 1, 4, 2, 1]` | 4 з 5 | 80.0% | 20.0% (зразок `3`) |
| **Bootstrap 3** | `[3, 5, 4, 4, 4]` | 3 з 5 | 60.0% | 40.0% (зразки `1`, `2`) |
| **Bootstrap 4** | `[4, 3, 1, 5, 3]` | 4 з 5 | 80.0% | 20.0% (зразок `2`) |
| **Bootstrap 5** | `[3, 2, 1, 5, 4]` | 5 з 5 | 100.0% | 0.0% (усі увійшли) |

::alert{type="info"}
**Теоретична закономірність:** При $N \\to \\infty$ ймовірність потрапляння спостереження у bootstrap-вибірку прямує до $1 - (1 - 1/N)^N \\approx 1 - 1/e \\approx \\mathbf{63.2\\%}$. Решта **$\\approx 36.8\\%$** залишаються поза вибіркою (Out-of-Bag) та утворюють природний тестовий набір.
::
::"""

    # 2. Output 2: RF Regressor (100 trees)
    old_out2 = """#output
🌲 Random Forest Regressor (100 дерев):
  R² train: 0.9723
  R² test:  0.8145
  MAE test: 0.3234
  RMSE test: 0.4489
  Gap: 0.1578
  Час навчання: 2.34s
::"""

    new_out2 = """#output
| Метрика ефективності регресії | Навчальна (Train) | Тестова (Test) | Різниця (Gap) | Швидкодія моделі |
| :--- | :---: | :---: | :---: | :---: |
| **Коефіцієнт детермінації ($R^2$)** | `0.9723` | **`0.8145`** | `0.1578` | **`2.34 с`** (`n_jobs=-1`) |
| **Середня абсолютна помилка (MAE)** | `0.1894` | **`0.3234`** | `+0.1340` | Середня похибка ~`$32,340` |
| **Корінь із середньоквадратичної (RMSE)** | `0.2645` | **`0.4489`** | `+0.1844` | Зниження похибки на 27% порівняно з деревом |

::alert{type="success"}
**Якісний стрибок ансамблю:** Об'єднання 100 дерев підняло тестовий коефіцієнт $R^2$ із **0.6523** (одиночне дерево) до **0.8145**. Ансамбль усуває дисперсію та забезпечує високу узагальнюючу якість.
::
::"""

    # 3. Output 3: n_estimators experiment
    old_out3 = """#output
![2 графіки: R² зростає та стабілізується, час навчання зростає лінійно]

📊 Висновки:
  • 10 дерев: R² test = 0.7823, час = 0.28s
  • 100 дерев: R² test = 0.8145, час = 2.34s
  • 500 дерев: R² test = 0.8167, час = 11.23s

✅ Оптимум: 100-200 дерев (баланс якість/швидкість)
::"""

    new_out3 = """#output
![Вплив кількості дерев n_estimators на якість прогнозу R2 та час навчання ансамблю Random Forest](/images/ai-python/decision-trees-random-forest/random-forest/01.png){.diagram-img}

| Кількість дерев (`n_estimators`) | $R^2_{\\text{test}}$ | Час навчання ($t$) | Приріст точності | Оцінка ефективності |
| :---: | :---: | :---: | :---: | :--- |
| **10** | `0.7823` | `0.28 с` | Базовий | Швидкий бейзлайн для чорнового оцінювання |
| **50** | `0.8062` | `1.18 с` | `+0.0239` | Помітне зростання точності |
| **100** | **`0.8145`** | **`2.34 с`** | `+0.0083` | 🏆 **Оптимальний вибір** за якістю та часом |
| **200** | `0.8160` | `4.62 с` | `+0.0015` | Висока стабільність, насичення точності |
| **500** | `0.8167` | `11.23 с` | `+0.0007` | П'ятикратне зростання часу заради 0.07% приросту |

::alert{type="info"}
**Практичний висновок:** Після 100–200 дерев крива тестової якості виходить на пологе плато. Збільшення розміру лісу понад 200 дерев не дає суттєвої користі, проте відчутно збільшує час тренування та інференсу.
::
::"""

    # 4. Output 4: Feature Importance
    old_out4 = """#output
📊 Важливість ознак (Feature Importance):

       Feature  Importance
        MedInc      0.5234
      Latitude      0.1456
     Longitude      0.1234
      AveRooms      0.0823
    Population      0.0512
      HouseAge      0.0445
     AveBedrms      0.0189
      AveOccup      0.0107

![Горизонтальна стовпчикова діаграма важливості ознак]
::"""

    new_out4 = """#output
![Горизонтальна діаграма важливості ознак Feature Importance для ансамблю Random Forest](/images/ai-python/decision-trees-random-forest/random-forest/02.png){.diagram-img}

| Ранг | Ознака датасету | Український переклад | Важливість (Gini Importance) | Частка впливу на прогноз |
| :---: | :--- | :--- | :---: | :--- |
| **1** | `MedInc` | Медіанний дохід жителів району ($10k) | **`0.5234`** | `52.34%` (домінуючий фактор) |
| **2** | `AveOccup` | Середня заселеність (мешканців на житло) | **`0.1456`** | `14.56%` |
| **3** | `Latitude` | Географічна широта кварталу | **`0.1234`** | `12.34%` (локація) |
| **4** | `Longitude` | Географічна довгота кварталу | **`0.0823`** | `8.23%` (наближеність до океану) |
| **5** | `HouseAge` | Середній вік будинків (роки) | **`0.0512`** | `5.12%` |
| **6** | `AveRooms` | Середня кількість кімнат | **`0.0445`** | `4.45%` |
| **7** | `Population` | Загальна чисельність населення | **`0.0189`** | `1.89%` |
| **8** | `AveBedrms` | Середня кількість спалень | **`0.0107`** | `1.07%` |

::alert{type="tip"}
**Відбір ознак (Feature Selection):** Ознаки з вагою менше $2\%$ (`Population`, `AveBedrms`) можна виключити з моделі без втрати точності передбачень, що зменшить розмір лісу в пам'яті та прискорить розрахунки.
::
::"""

    # 5. Output 5: OOB Score
    old_out5 = """#output
📊 Out-of-Bag Score:
  OOB Score (R²): 0.8089
  R² test:        0.8145

💡 OOB Score ≈ R² test — не потрібен окремий validation set!
::"""

    new_out5 = """#output
| Метод валідації | Оцінка детермінації ($R^2$) | Необхідність Test Set | Характеристика процедури |
| :--- | :---: | :---: | :--- |
| **Out-of-Bag (OOB) Score** | **`0.8089`** | ❌ Не потрібен | Обчислено автоматично на ~37% невикористаних bootstrap-зразків для кожного дерева |
| **Окремий Test Set (hold-out 20%)** | **`0.8145`** | ✅ Потрібен | Традиційна оцінка на незалежній відкладеній вибірці |
| **Розходження оцінок ($\Delta$)** | **`0.0056`** | — | Відхилення менше $0.6\%$ свідчить про високу надійність OOB-оцінки |

::alert{type="success"}
**Перевага OOB:** Out-of-Bag Score дозволяє отримувати точну та об'єктивну оцінку якості без виділення окремої валідаційної вибірки, що особливо цінно на невеликих датасетах.
::
::"""

    # 6. Output 6: Iris RF Classifier
    old_out6 = """#output
🌲 Random Forest Classifier (Iris):
  Accuracy: 1.0000

              precision    recall  f1-score   support

      setosa       1.00      1.00      1.00        19
  versicolor       1.00      1.00      1.00        13
   virginica       1.00      1.00      1.00        13

    accuracy                           1.00        45
   macro avg       1.00      1.00      1.00        45
weighted avg       1.00      1.00      1.00        45

📊 Ймовірності для перших 3 зразків:
  Зразок 1: [0.   0.01 0.99] → клас 2
  Зразок 2: [1. 0. 0.] → клас 0
  Зразок 3: [0.   0.99 0.01] → клас 1
::"""

    new_out6 = """#output
| Сорт Iris (клас) | Precision | Recall | F1-Score | Кількість зразків (Support) |
| :--- | :---: | :---: | :---: | :---: |
| **Iris-setosa** | `1.0000` | `1.0000` | `1.0000` | 19 |
| **Iris-versicolor** | `1.0000` | `1.0000` | `1.0000` | 13 |
| **Iris-virginica** | `1.0000` | `1.0000` | `1.0000` | 13 |
| **Загальна точність (Accuracy)** | — | — | **`1.0000`** | 45 |
| **Macro Average** | `1.0000` | `1.0000` | `1.0000` | 45 |
| **Weighted Average** | `1.0000` | `1.0000` | `1.0000` | 45 |

**Розподіл передбачених ймовірностей для перших 3 тестових зразків:**

| Тестовий зразок | $P(\\text{Setosa})$ | $P(\\text{Versicolor})$ | $P(\\text{Virginica})$ | Передбачений клас | Фактичний клас |
| :---: | :---: | :---: | :---: | :---: | :---: |
| **Зразок №1** | `0.00` | `0.01` | **`0.99`** | `2` (*Virginica*) | `2` (*Virginica*) |
| **Зразок №2** | **`1.00`** | `0.00` | `0.00` | `0` (*Setosa*) | `0` (*Setosa*) |
| **Зразок №3** | `0.00` | **`0.99`** | `0.01` | `1` (*Versicolor*) | `1` (*Versicolor*) |

::alert{type="success"}
**Бездоганна класифікація:** Random Forest досяг **$100\\%$ точності** на тестовій вибірці Iris. Консенсус 100 дерев дає впевнені ймовірності прогнозу ($99-100\\%$).
::
::"""

    replacements = [
        ("Code 1 (modern rng)", old_code1, new_code1),
        ("Output 1 (Bootstrap)", old_out1, new_out1),
        ("Output 2 (RF Regressor)", old_out2, new_out2),
        ("Output 3 (n_estimators)", old_out3, new_out3),
        ("Output 4 (Feature Importance)", old_out4, new_out4),
        ("Output 5 (OOB Score)", old_out5, new_out5),
        ("Output 6 (Iris RF)", old_out6, new_out6),
    ]

    for name, old, new in replacements:
        if old not in content:
            print(f"[ERROR] Could not find {name} in 02.random-forest.md")
            sys.exit(1)
        count = content.count(old)
        if count > 1:
            print(f"[ERROR] Multiple occurrences ({count}) of {name} in 02.random-forest.md")
            sys.exit(1)
        content = content.replace(old, new)
        print(f"[OK] Replaced {name}")

    with open(filepath, "w", encoding="utf-8") as f:
        f.write(content)
    print("Successfully updated 02.random-forest.md!")

if __name__ == "__main__":
    update_decision_trees()
    update_random_forest()
