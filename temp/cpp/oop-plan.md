# План вивчення ООП у C++ — серія статей

## Огляд серії

ООП — центральна парадигма сучасного C++, яка вимагає послідовного, глибокого розкриття від мотивації до розвинутих механізмів.
Серія складається з **18 статей** (~1000 рядків кожна), з нумерацією `49`–`66`.

**Пріоритетне джерело порядку:** [___TOC-13-14-15.md](file:///Users/arakviel/Work/kostyl.dev/temp/cpp/oop/___TOC-13-14-15.md)
**Додаткові джерела:** файли `01`–`18` з `temp/cpp/oop/`
**Стиль коду:** [02.code-style.md](file:///Users/arakviel/Work/kostyl.dev/content/02.cpp/02.code-style.md) (Java-подібний: camelCase, Allman braces, PascalCase класи, `m_` для полів, 4 пробіли)
**Стиль матеріалів:** стиль існуючих статей `content/02.cpp/` + скіл [college-lectures](file:///Users/arakviel/Work/kostyl.dev/.agents/skills/college-lectures/SKILL.md)

---

## Стаття 49 — Введення в ООП: від процедур до об'єктів

**Файл:** `49.oop-intro.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `33.struct.md`, `34.struct-functions.md`, `36.struct-patterns.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 13 п.1–3; `__13-oop-intro.md`; `01. Введення-в-ООП`

### Навчальні цілі
- Показати обмеження процедурного підходу на прикладі `struct` + вільних функцій (зв'язок із статтями 33–36)
- Продемонструвати проблему зростання складності: формула зв'язків N×N
- Пояснити історичний контекст: C → Smalltalk → C++ → стандарти C++11/14/17/20
- Ввести ООП як спосіб моделювання «від об'єктів», а не «від завдань»
- Сформулювати три принципи ООП: інкапсуляція, наслідування (*inheritance*), поліморфізм (*polymorphism*) — оглядово

### Структура статті
1. Hook: рефакторинг коду з `struct Student` + `initStudent()` + `printStudent()` — зв'язок із серією struct
2. Проблема процедурного коду: дані і функції існують окремо, синхронізація вручну
3. Криза складності: N структур × M функцій — зв'язки N×N, `::plant-uml` діаграма залежностей
4. Історія: C → Simula → Smalltalk → C with Classes → C++ (Б. Страуструп, 1979–1985)
5. ООП як зміна парадигми: моделювання від об'єктів, а не від алгоритмів
6. Три стовпи ООП: інкапсуляція, наслідування, поліморфізм — оглядовий `::card-group`
7. Перший погляд: `driveTo(you, work)` vs `you.driveTo(work)` — зміна мислення
8. Резюме та анонс: «на наступному етапі ми перетворимо `struct` на `class`»

---

## Стаття 50 — Класи, об'єкти та методи

**Файл:** `50.classes-objects-methods.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `49.oop-intro.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 13 п.5; `__13-oop-intro.md`; `02. Класи,-Обєкти-і-Методи`

### Навчальні цілі
- Ввести ключове слово `class` та пояснити його зв'язок зі `struct`
- Визначити поняття об'єкта (екземпляра класу)
- Показати методи як функції, прив'язані до об'єкта (неявний об'єкт)
- Пояснити префікс `m_` для полів класу
- Продемонструвати порядок розміщення: `private`-поля → `public`-методи
- Показати зв'язок із STL (`std::string`, `std::vector` — вони теж класи)

### Структура статті
1. Hook: перетворення `struct Date` + `printDate()` на `class Date` з методом `print()`
2. Синтаксис `class`: оголошення, тіло, `;` після `}`
3. Поля (data members) та методи (member functions): терміни
4. Створення об'єкта: `Date today;` — інстанціювання (*instantiation*)
5. Виклик методу: `today.print()` — оператор `.`, неявний об'єкт
6. Конвенція `m_` для полів; PascalCase для класів; camelCase для методів
7. Порядок секцій: `private` → `public` (або `public` → `private` — обговорення обох підходів)
8. Реальні класи навколо: `std::string`, `std::cout`, `std::vector` — все це класи STL
9. Практичне завдання: клас `Numbers` (Модуль 13 п.6), `::accordion`
10. Резюме

---

## Стаття 51 — Специфікатори доступу та інкапсуляція

**Файл:** `51.access-specifiers-encapsulation.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `50.classes-objects-methods.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 13 п.4; `03. Специфікатори-доступу`; `04. Інкапсуляція,-Геттери-і-Сеттери`

### Навчальні цілі
- Глибоко пояснити `public`, `private` (та `protected` — оглядово)
- Розкрити інкапсуляцію як фундаментальний принцип ООП
- Показати геттери (*getters*) і сеттери (*setters*) як механізм контрольованого доступу
- Пояснити переваги інкапсуляції: спрощення, захист, гнучкість зміни реалізації
- Продемонструвати `struct` vs `class` — єдина різниця: доступ за замовчуванням

### Структура статті
1. Hook: аналогія з пультом телевізора — інтерфейс vs реалізація
2. `public` і `private`: синтаксис, семантика, помилка компіляції при зверненні до `private`
3. `struct` vs `class`: лише різниця доступу за замовчуванням (`public` vs `private`), `::code-group`
4. Інкапсуляція (*encapsulation*): приховування даних, публічний інтерфейс
5. Чому поля мають бути `private`: 4 переваги (спрощення, захист, гнучкість, валідація)
6. Геттери і сеттери: `getRadius()`, `setRadius(double r)` — із валідацією
7. Практика: рефакторинг відкритої структури у повністю інкапсульований клас, `::accordion`
8. Резюме

---

## Стаття 52 — Конструктори

**Файл:** `52.constructors.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `51.access-specifiers-encapsulation.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.1; `05. Конструктори`; `__14-oop-2.md`

### Навчальні цілі
- Пояснити проблему неініціалізованих об'єктів
- Ввести конструктор як спеціальний метод, що гарантує коректний початковий стан
- Показати конструктор за замовчуванням (*default constructor*)
- Показати параметризований конструктор
- Зменшення кількості конструкторів через параметри за замовчуванням
- Пояснити неявний конструктор за замовчуванням та його зникнення при оголошенні власного
- Uniform initialization: `{}` vs `()`

### Структура статті
1. Hook: об'єкт `Fraction` без конструктора — чисельник і знаменник неініціалізовані
2. Конструктор: ім'я збігається з ім'ям класу, немає типу повернення
3. Конструктор за замовчуванням: без параметрів, `Fraction() { m_numerator = 0; m_denominator = 1; }`
4. Параметризований конструктор: `Fraction(int numerator, int denominator)`
5. Зменшення кількості: параметри за замовчуванням, `Fraction(int num = 0, int den = 1)`
6. Неявний дефолтний конструктор: коли є, коли зникає
7. Правило: «завжди створюйте хоча б один конструктор»
8. `::debugger-view` — стан об'єкта після конструктора
9. Практичне завдання: клас `Ball` (Модуль 14 п.1), `::accordion`
10. Резюме

---

## Стаття 53 — Списки ініціалізації та делегуючі конструктори

**Файл:** `53.init-lists-delegating.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `52.constructors.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.2–4; `06. Список-ініціалізації`; `07. Ініціалізація-нестатичних`; `08. Делегуючі-конструктори`

### Навчальні цілі
- Пояснити Member Initializer List (MIL) — синтаксис `: m_field(value)`
- Показати обов'язкові випадки: `const`-поля, посилання, масиви
- Пояснити порядок ініціалізації: за порядком оголошення полів, не за MIL
- Розкрити Non-Static Member Initialization (NSMI, C++11)
- Ввести делегуючі конструктори (*constructor delegation*)
- Порівняти три підходи ініціалізації: тіло конструктора vs MIL vs NSMI

### Структура статті
1. Hook: конструктор із присвоєнням у тілі vs MIL — що ефективніше?
2. MIL: синтаксис, семантика (ініціалізація, а не присвоєння)
3. Обов'язковість MIL: `const`-поля, посилання `&`, об'єктні поля без дефолтного конструктора
4. Порядок ініціалізації: за порядком оголошення; `::warning` про порушення
5. NSMI (C++11): значення за замовчуванням прямо біля поля — `int m_count = 0;`
6. Делегуючі конструктори: один конструктор викликає інший — `Fraction(int n) : Fraction(n, 1) {}`
7. `::code-group` — порівняння трьох підходів на прикладі одного класу
8. Практика та резюме

---

## Стаття 54 — Деструктори та ідіома RAII

**Файл:** `54.destructors-raii.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `53.init-lists-delegating.md`, `19.dynamic-memory.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.5; `09. Деструктори`

### Навчальні цілі
- Пояснити, коли і чому об'єкт потрібно «прибирати» (динамічна пам'ять, файли)
- Ввести деструктор: `~ClassName()` — синтаксис, автоматичний виклик
- Розкрити порядок виклику деструкторів: зворотний до порядку створення
- Сформулювати ідіому RAII (*Resource Acquisition Is Initialization*)
- Пояснити небезпеку `exit()` — деструктори не викликаються
- Показати практичний приклад: клас, що керує динамічним масивом

### Структура статті
1. Hook: `new int[100]` без `delete` — витік пам'яті; «хто за цим прибирає?»
2. Деструктор: ім'я `~ClassName()`, немає параметрів і типу повернення
3. Автоматичний виклик: при виході зі scope (стек), при `delete` (купа)
4. Порядок деструкції: зворотний до конструкції, `::plant-uml` sequence diagram
5. Ідіома RAII: ресурс захоплюється в конструкторі, звільняється в деструкторі
6. Практика RAII: клас `IntArray` з `new[]` у конструкторі, `delete[]` у деструкторі
7. `::caution` — `exit()` обходить деструктори
8. `::debugger-view` — стан об'єкта перед і після деструкції
9. Практика та резюме

---

## Стаття 55 — Прихований покажчик `this`

**Файл:** `55.this-pointer.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `54.destructors-raii.md`, `15.pointers-basics.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.6; `10. Прихований-вказівник-this`

### Навчальні цілі
- Пояснити механізм: як метод знає, до якого об'єкта він належить
- Розкрити `this` як неявний покажчик на поточний об'єкт
- Показати явне використання `this->` для розв'язання конфліктів імен
- Ввести ланцюговий виклик (*method chaining*): `return *this;`
- Продемонструвати `this` у конструкторі та деструкторі

### Структура статті
1. Hook: два об'єкти `Fraction f1, f2;` — один метод `print()`, різні дані — як?
2. Неявний покажчик `this`: компілятор додає `Fraction* const this` першим параметром
3. `::plant-uml` — як `f1.print()` перетворюється на `print(&f1)` під капотом
4. Явне використання `this->m_numerator` — розв'язання конфліктів імен
5. Method chaining: `Calc& add(int v) { m_value += v; return *this; }` → `calc.add(5).sub(3).mul(2)`
6. `this` у конструкторі — об'єкт ще в процесі створення
7. `::debugger-view` — значення `this` для різних об'єктів
8. Практика та резюме

---

## Стаття 56 — Розподіл класу на заголовковий файл та файл реалізації

**Файл:** `56.class-header-source.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `55.this-pointer.md`, `46.multifile-programs.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.7; `11. Класи-і-заголовкові-файли`

### Навчальні цілі
- Пов'язати з попередньою темою багатофайлових програм (стаття 46)
- Показати оголошення класу у `.h`, реалізацію методів у `.cpp`
- Пояснити оператор `::` (scope resolution) для визначення методів поза класом
- Розкрити include guards / `#pragma once`
- Показати inline-методи та їх правила

### Структура статті
1. Hook: клас `Date` з 500 рядків у одному файлі — нечитабельно
2. Оголошення vs визначення: `.h` = «що клас уміє», `.cpp` = «як він це робить»
3. `Date.h`: оголошення класу, прототипи методів, `#pragma once`
4. `Date.cpp`: `#include "Date.h"`, визначення методів через `Date::print()`
5. `main.cpp`: `#include "Date.h"`, створення об'єктів, виклик методів
6. `::code-tree` — структура проєкту з трьома файлами
7. Inline-методи: визначення в `.h` — коли допустимо, коли ні
8. Правило одного визначення (*ODR, One Definition Rule*)
9. Практика: рефакторинг класу `IntArray` на `.h` + `.cpp`, `::accordion`
10. Резюме

---

## Стаття 57 — Класи та `const`

**Файл:** `57.const-classes.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `56.class-header-source.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.8; `12. Класи-і-const`

### Навчальні цілі
- Пояснити константні об'єкти: `const Date today(2025, 9, 16);`
- Ввести константні методи: `int getYear() const;`
- Показати, що `const` об'єкт може викликати лише `const`-методи
- Пояснити `const` і `this`: у `const`-методі `this` має тип `const ClassName*`
- Перевантаження за `const`: `operator[]` у двох версіях

### Структура статті
1. Hook: `const Fraction f(3, 5); f.print();` — помилка компіляції — чому?
2. Константний об'єкт: обіцянка не змінювати стан
3. Константний метод: суфікс `const` після `()`, не модифікує `m_`-поля
4. Правило: геттери завжди `const`, мутатори — ніколи
5. `const` покажчик `this`: у `const`-методі `this` → `const Fraction*`
6. Перевантаження за `const`: дві версії `operator[]` — для запису і для читання, `::code-group`
7. `::warning` — типові помилки: не-`const` метод на `const` об'єкті
8. Практика та резюме

---

## Стаття 58 — Статичні члени класу

**Файл:** `58.static-members.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `57.const-classes.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.9; `13. Статичні-змінні`; `14. Статичні-методи`

### Навчальні цілі
- Пояснити різницю між даними об'єкта та даними класу
- Ввести `static`-поля: одна копія на весь клас, ініціалізація поза класом
- Ввести `static`-методи: не мають `this`, доступ лише до `static`-полів
- Показати практичні патерни: лічильник об'єктів, генератор ID, фабричний метод
- Inline static (C++17): `inline static int s_count = 0;`

### Структура статті
1. Hook: «скільки об'єктів `Student` було створено?» — звичайне поле не допоможе
2. `static`-поле: належить класу, а не об'єкту; `::plant-uml` — пам'ять
3. Ініціалізація `static`-поля: поза класом у `.cpp`; `inline static` (C++17)
4. `static`-метод: виклик через `ClassName::method()`, немає `this`
5. Обмеження: `static`-метод не має доступу до `m_`-полів
6. Патерн «лічильник об'єктів»: `s_count++` у конструкторі, `s_count--` у деструкторі
7. Патерн «генератор ID»: `static int s_nextId;` — кожен об'єкт отримує унікальний ID
8. `::code-group` — порівняння доступу: через об'єкт vs через клас
9. Практика та резюме

---

## Стаття 59 — Конструктор копіювання та запобігання копіюванню

**Файл:** `59.copy-constructor.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `58.static-members.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.10

### Навчальні цілі
- Показати, коли відбувається копіювання об'єкта (передача за значенням, повернення, ініціалізація)
- Ввести конструктор копіювання: `ClassName(const ClassName& other)`
- Пояснити неявний конструктор копіювання — порогове копіювання (*shallow copy*)
- Показати проблему подвійного `delete` при shallow copy з динамічною пам'яттю
- Ввести `= delete` для заборони копіювання
- Анонсувати глибоке копіювання (стаття 66)

### Структура статті
1. Hook: `Fraction f2 = f1;` — що відбувається? Скільки об'єктів?
2. Коли C++ копіює об'єкт: 3 сценарії (ініціалізація, аргумент, повернення)
3. Конструктор копіювання: сигнатура, параметр `const ClassName&`
4. Неявний конструктор: shallow copy — побітове копіювання полів
5. Проблема: `IntArray a; IntArray b = a;` — обидва вказують на один масив → подвійний `delete`
6. `::memory-view` — два об'єкти, один блок пам'яті
7. Заборона копіювання: `ClassName(const ClassName&) = delete;`
8. Анонс: «у статті 66 ми реалізуємо глибоке копіювання»
9. Практика та резюме

---

## Стаття 60 — Дружні функції та класи

**Файл:** `60.friend-functions-classes.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `59.copy-constructor.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 14 п.11; `15. Дружні-функції-і-класи`

### Навчальні цілі
- Пояснити, чому іноді зовнішня функція потребує доступу до `private`-членів
- Ввести `friend`-функції: синтаксис, семантика, оголошення всередині класу
- Ввести `friend`-класи: один клас дружить з іншим
- Показати, що `friend` не порушує інкапсуляцію, а є контрольованим розширенням інтерфейсу
- Обговорити обмежене використання: `friend` — не «hack», але й не норма

### Структура статті
1. Hook: функція `isEqual(Fraction a, Fraction b)` — не може дістатися до `m_numerator`
2. `friend`-функція: `friend bool isEqual(const Fraction& a, const Fraction& b);`
3. `friend`-функція — не метод класу: немає `this`, не належить класу
4. Множинна дружба: функція — `friend` для кількох класів
5. `friend`-клас: `friend class Display;` — Display бачить `private`-поля Fraction
6. Властивості: дружба не транзитивна, не наслідується, не взаємна
7. `::warning` — зловживання `friend` руйнує інкапсуляцію
8. Головне застосування `friend`: оператори `<<` і `>>` (анонс перевантаження)
9. Практика та резюме

---

## Стаття 61 — Анонімні об'єкти та вкладені типи

**Файл:** `61.anonymous-objects-nested-types.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `60.friend-functions-classes.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.1–2; `16. Анонімні-обєкти`; `17. Вкладені-типи`; `__15-oop-3.md`

### Навчальні цілі
- Пояснити анонімні (тимчасові) об'єкти: `Dollars(5)` — створення без імені
- Показати час життя анонімних об'єктів: до кінця виразу
- Використання: передача аргументів, повернення з функцій, одноразові операції
- Ввести вкладені типи: `enum`, `struct`, `class`, `using` всередині класу
- Пояснити доступ через `::` — `Fruit::FruitType::APPLE`
- Інкапсуляція допоміжних типів: зменшення забруднення глобального простору імен

### Структура статті
1. Hook: `print(Dollars(5));` — навіщо створювати змінну, яка використовується один раз?
2. Анонімний об'єкт: синтаксис, час життя, автоматична деструкція
3. Застосування: повернення з функцій, аргументи, тернарний оператор
4. `::note` — порівняння: іменований vs анонімний — коли що обрати
5. Вкладені типи: `enum` всередині класу — `Fruit::APPLE`
6. Вкладені `struct` / `class`: допоміжні типи, приватні для реалізації
7. Вкладені `using` / `typedef`: скорочення складних типів
8. `::plant-uml` — діаграма класу з вкладеним enum
9. Практика та резюме

---

## Стаття 62 — Вимірювання часу виконання: RAII-таймер

**Файл:** `62.timing-raii-timer.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `61.anonymous-objects-nested-types.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.3; `18. Вимірювання-часу`; `__15-oop-3.md`

### Навчальні цілі
- Познайомити з бібліотекою `<chrono>`: `steady_clock`, `high_resolution_clock`
- Побудувати RAII-клас `Timer`: конструктор фіксує час, деструктор обчислює різницю
- Продемонструвати використання вкладених типів (`using clock_t`, `using second_t`)
- Показати практичне застосування: порівняння алгоритмів сортування
- Закріпити ідіому RAII на реальному прикладі

### Структура статті
1. Hook: «який алгоритм сортування швидший — бульбашковий чи вставками?» — як виміряти?
2. `<chrono>`: ієрархія: `clock → time_point → duration`
3. Ручний вимір: `auto start = ...; auto end = ...; diff = end - start;`
4. RAII-таймер: конструктор зберігає `now()`, деструктор виводить `elapsed()`
5. Вкладені `using`: `using clock_t = std::chrono::high_resolution_clock;`
6. `::terminal-preview` — вивід: `Elapsed: 0.00342 seconds`
7. Практика: бенчмарк `bubbleSort` vs `insertionSort` на 10 000 елементів
8. `::tip` — «не вимірюйте в Debug-режимі, лише Release»
9. Резюме

---

## Стаття 63 — Перевантаження операторів: основи та арифметика

**Файл:** `63.operator-overloading-basics.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `62.timing-raii-timer.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.4–5; `__15-oop-3.md`

### Навчальні цілі
- Пояснити, чому вбудовані оператори не працюють з користувацькими типами
- Ввести перевантаження операторів (*operator overloading*) — загальні правила та обмеження
- Показати перевантаження через `friend`-функції: `operator+`, `operator-`, `operator*`
- Пояснити комутативність: `Fraction * 2` та `2 * Fraction`
- Продемонструвати перевантаження `operator<<` та `operator>>` для потоків

### Структура статті
1. Hook: `Fraction f1(1,2), f2(3,4); auto f3 = f1 + f2;` — помилка — «а хочемо, щоб працювало»
2. Фундаментальні правила: не можна створювати нові оператори, не можна змінювати пріоритет
3. Оператори, які **не** можна перевантажити: `::`, `sizeof`, `?:`, `.`
4. Через `friend`-функцію: `friend Fraction operator+(const Fraction& a, const Fraction& b);`
5. Комутативність: `friend Fraction operator*(const Fraction& f, int n);` + `friend Fraction operator*(int n, const Fraction& f);`
6. Оператори потоків: `friend std::ostream& operator<<(std::ostream& out, const Fraction& f);`
7. `friend std::istream& operator>>(std::istream& in, Fraction& f);`
8. `::code-group` — перевантаження через `friend` vs через вільну функцію
9. Практика та резюме

---

## Стаття 64 — Перевантаження операторів: методи класу та спеціальні оператори

**Файл:** `64.operator-overloading-methods.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `63.operator-overloading-basics.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.6–11

### Навчальні цілі
- Показати перевантаження через методи класу: `Fraction operator-() const;`
- Пояснити, які оператори **повинні** бути методами: `=`, `[]`, `()`, `->`, conversions
- Ввести унарні оператори: `-`, `!`
- Показати оператори порівняння: `==`, `!=`, `<`, `<=`, `>`, `>=`
- Розкрити префіксний і постфіксний `++` / `--`: різниця сигнатур (int dummy)
- Ввести `operator[]`: l-value версія та `const`-версія
- Ввести `operator()` та поняття функтора (*functor*)

### Структура статті
1. Hook: `Fraction f(3,4); Fraction neg = -f;` — унарний мінус
2. Перевантаження через метод: неявний `this` = лівий операнд
3. Оператори, що обов'язково є методами: `=`, `[]`, `()`, `->`
4. Унарні: `operator-()`, `operator!()` — як `const`-методи
5. Порівняння: `operator==`, `operator<` — решта через них
6. Інкремент/декремент: `operator++()` (prefix) vs `operator++(int)` (postfix)
7. `operator[]`: `int& operator[](int index);` і `const int& operator[](int index) const;`
8. `operator()`: функтор — об'єкт, що поводиться як функція; зв'язок з лямбдами (стаття 25)
9. `::code-group` — friend-функція vs метод — коли що обирати
10. Практика та резюме

---

## Стаття 65 — Перевантаження приведення типів, `explicit` та `delete`

**Файл:** `65.typecast-explicit-delete.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `64.operator-overloading-methods.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.12–13; `__15-oop-3.md`

### Навчальні цілі
- Показати перевантаження приведення типів (*user-defined type conversion*)
- Пояснити неявні конвертації: `Fraction` → `double` через `operator double()`
- Розкрити небезпеку неявних конвертацій: несподівані виклики конструкторів
- Ввести `explicit`: заборона неявних конвертацій і для конструкторів, і для операторів
- Показати `= delete` для заборони конкретних операцій (конвертацій, перевантажень)

### Структура статті
1. Hook: `void print(Fraction f);` → `print(5);` — чому це компілюється?
2. Неявний конструктор-конвертер: `Fraction(int n)` дозволяє `Fraction f = 5;`
3. `explicit`-конструктор: заборона неявного перетворення
4. Перевантаження приведення: `operator double() const { return (double)m_num / m_den; }`
5. `explicit operator bool()` — безпечний патерн «перевірка у `if`»
6. `= delete` для операцій: `void foo(int) = delete;` — заборона конкретних перевантажень
7. `::warning` — неявні конвертації як джерело тонких багів
8. `::code-group` — `explicit` vs не-`explicit` — поведінка компілятора
9. Практика та резюме

---

## Стаття 66 — Глибоке копіювання та оператор присвоювання

**Файл:** `66.deep-copy-assignment.md`
**Орієнтовний обсяг:** ~1000 рядків
**Залежності:** `65.typecast-explicit-delete.md`, `59.copy-constructor.md`
**Джерела:** `___TOC-13-14-15.md` → Модуль 15 п.14; `__15-oop-3.md`

### Навчальні цілі
- Повернутися до проблеми shallow copy з динамічною пам'яттю (стаття 59)
- Реалізувати глибоке копіювання (*deep copy*): виділення нового блоку пам'яті
- Перевантажити `operator=`: перевірка самоприсвоювання, звільнення старих ресурсів, виділення нових
- Сформулювати «Правило трьох» (*Rule of Three*): деструктор + конструктор копіювання + `operator=`
- Анонсувати «Правило п'яти» (move-семантика — у майбутній серії)

### Структура статті
1. Hook: `IntArray a(10); IntArray b = a;` → crash при виході зі scope — пригадування статті 59
2. Shallow vs Deep copy: `::memory-view` — два об'єкти, два окремих блоки пам'яті
3. Deep copy у конструкторі копіювання: `new int[m_size]`, `std::copy`
4. `operator=`: `IntArray& operator=(const IntArray& other);`
5. Перевірка самоприсвоювання: `if (this == &other) return *this;`
6. Алгоритм: `delete[]` старе → виділити нове → скопіювати → повернути `*this`
7. Правило трьох: якщо потрібен один із трьох (деструктор, copy ctor, `operator=`) — потрібні всі три
8. `::note` — Правило п'яти (move constructor + move assignment) — анонс
9. `::plant-uml` — діаграма пам'яті: shallow vs deep
10. Практика та резюме

---

## Зведена таблиця

| Файл | Ключова тема | Джерела (TOC) | Нові компоненти |
|------|-------------|---------------|-----------------|
| `49.oop-intro.md` | Мотивація ООП, історія, три принципи | Модуль 13 п.1–3 | `::plant-uml`, `::card-group` |
| `50.classes-objects-methods.md` | `class`, об'єкт, метод, `m_`-конвенція | Модуль 13 п.5–6 | `::code-group`, `::accordion` |
| `51.access-specifiers-encapsulation.md` | `public`/`private`, інкапсуляція, геттери/сеттери | Модуль 13 п.4 | `::code-group` |
| `52.constructors.md` | Конструктори, параметри за замовчуванням | Модуль 14 п.1 | `::debugger-view` |
| `53.init-lists-delegating.md` | MIL, NSMI, делегуючі конструктори | Модуль 14 п.2–4 | `::code-group`, `::warning` |
| `54.destructors-raii.md` | Деструктори, RAII | Модуль 14 п.5 | `::plant-uml`, `::caution` |
| `55.this-pointer.md` | `this`, method chaining | Модуль 14 п.6 | `::debugger-view`, `::plant-uml` |
| `56.class-header-source.md` | `.h` + `.cpp`, `::`, ODR | Модуль 14 п.7 | `::code-tree` |
| `57.const-classes.md` | `const` об'єкти, `const`-методи | Модуль 14 п.8 | `::code-group`, `::warning` |
| `58.static-members.md` | `static`-поля, `static`-методи | Модуль 14 п.9 | `::plant-uml`, `::code-group` |
| `59.copy-constructor.md` | Конструктор копіювання, `= delete` | Модуль 14 п.10 | `::memory-view` |
| `60.friend-functions-classes.md` | `friend`-функції, `friend`-класи | Модуль 14 п.11 | `::warning` |
| `61.anonymous-objects-nested-types.md` | Анонімні об'єкти, вкладені типи | Модуль 15 п.1–2 | `::plant-uml`, `::note` |
| `62.timing-raii-timer.md` | `<chrono>`, RAII-таймер | Модуль 15 п.3 | `::terminal-preview`, `::tip` |
| `63.operator-overloading-basics.md` | Перевантаження: `+`, `-`, `*`, `<<`, `>>` | Модуль 15 п.4–5 | `::code-group` |
| `64.operator-overloading-methods.md` | `[]`, `()`, `++`, `--`, порівняння, функтори | Модуль 15 п.6–11 | `::code-group` |
| `65.typecast-explicit-delete.md` | Приведення типів, `explicit`, `= delete` | Модуль 15 п.12–13 | `::code-group`, `::warning` |
| `66.deep-copy-assignment.md` | Deep copy, `operator=`, Правило трьох | Модуль 15 п.14 | `::memory-view`, `::plant-uml` |

---

## Відповідність джерел → статей

| Джерело (temp/cpp/oop/) | Інтегровано в статтю |
|--------------------------|---------------------|
| `01. Введення-в-ООП` | 49 |
| `02. Класи,-Обєкти-і-Методи` | 50 |
| `03. Специфікатори-доступу` | 51 |
| `04. Інкапсуляція,-Геттери-і-Сеттери` | 51 |
| `05. Конструктори` | 52 |
| `06. Список-ініціалізації` | 53 |
| `07. Ініціалізація-нестатичних-членів` | 53 |
| `08. Делегуючі-конструктори` | 53 |
| `09. Деструктори` | 54 |
| `10. Прихований-вказівник-this` | 55 |
| `11. Класи-і-заголовкові-файли` | 56 |
| `12. Класи-і-const` | 57 |
| `13. Статичні-змінні` | 58 |
| `14. Статичні-методи` | 58 |
| `15. Дружні-функції-і-класи` | 60 |
| `16. Анонімні-обєкти` | 61 |
| `17. Вкладені-типи-даних` | 61 |
| `18. Вимірювання-часу` | 62 |
| `__13-oop-intro.md` | 49, 50, 51 |
| `__14-oop-2.md` | 52–60 |
| `__15-oop-3.md` | 61–66 |

---

## Наскрізний наративний зв'язок

Серія побудована як **єдина історія**: від «чому `struct` + функції — це ще не ООП» (ст. 49) через поступове введення механізмів класів (ст. 50–60) до розвинутих технік перевантаження та управління ресурсами (ст. 61–66). Кожна стаття починається з концептуального мосту до попередньої та завершується анонсом наступної.

**Наскрізний приклад:** клас `Fraction` (дріб) проходить через більшість статей, поступово збагачуючись: спочатку — поля, потім — конструктори, інкапсуляція, `const`, оператори, глибоке копіювання. Допоміжні приклади: `IntArray` (RAII, deep copy), `Date` (заголовкові файли), `Timer` (chrono).
