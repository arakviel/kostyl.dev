# ООП в C++: Покажчик this, конструктор копіювання, статичні члени та дружні функції

> **Викладач:** Верещагін О.О.  
> **Тема:** Конструктори (зменшення кількості, списки ініціалізації, делегування), деструктори і RAII, покажчик `this`, розподіл на `.h` і `.cpp`, ключове слово `const`, статичні поля і методи, конструктор копіювання, дружні функції та класи.

---

## 1. Зменшення кількості конструкторів

Коли в класі є кілька конструкторів (наприклад, за замовчуванням та параметризований), часто конструктор за замовчуванням є зайвим дублюванням, якщо надати параметри за замовчуванням у конструкторі з параметрами.

### Приклад: спрощення класу `Fraction`

Замість визначення двох окремих конструкторів:
```cpp
class Fraction
{
private:
    int m_numerator;
    int m_denominator;

public:
    // Конструктор за замовчуванням
    Fraction()
    {
        m_numerator = 0;
        m_denominator = 1;
    }

    // Конструктор з двома параметрами
    Fraction(int numerator, int denominator)
    {
        m_numerator = numerator;
        m_denominator = denominator;
    }
};
```

Можна звести їх до одного універсального конструктора:
```cpp
class Fraction
{
private:
    int m_numerator;
    int m_denominator;

public:
    // Єдиний конструктор з параметрами за замовчуванням
    Fraction(int numerator = 0, int denominator = 1)
    {
        m_numerator = numerator;
        m_denominator = denominator;
    }
};
```

Тепер цей конструктор покриває всі сценарії виклику:
```cpp
Fraction f1;          // 0/1
Fraction f2(6);       // 6/1
Fraction f3(2, 3);    // 2/3
```

> **Правило:** На практиці намагайтеся зменшувати кількість конструкторів вашого класу шляхом використання параметрів за замовчуванням.
> 
> **Неявний конструктор за замовчуванням:** Якщо ваш клас не має жодного визначеного конструктора, C++ автоматично згенерує публічний конструктор за замовчуванням без параметрів (default constructor). Якщо ви оголосили хоча б один свій конструктор, автоматична генерація вимикається.
> 
> **Правило:** Створюйте хоча б один конструктор у класі, навіть якщо це порожній конструктор за замовчуванням.

---

### Завдання №1: Клас `Ball`

Створити клас `Ball` з приватними полями:
- `m_color` (типу `std::string`, за замовчуванням `"red"`);
- `m_radius` (типу `double`, за замовчуванням `20.0`).

Потрібно реалізувати конструктори:
1. Для встановлення тільки `color`;
2. Для встановлення тільки `radius`;
3. Для встановлення обох значень;
4. Без параметрів (за замовчуванням).

#### Версія без параметрів за замовчуванням (4 окремих конструктори):
```cpp
#include <iostream>
#include <string>

class Ball
{
private:
    std::string m_color;
    double m_radius;

public:
    // 1. За замовчуванням
    Ball()
    {
        m_color = "red";
        m_radius = 20.0;
    }

    // 2. Лише колір
    Ball(const std::string &color)
    {
        m_color = color;
        m_radius = 20.0;
    }

    // 3. Лише радіус
    Ball(double radius)
    {
        m_color = "red";
        m_radius = radius;
    }

    // 4. Обидва параметри
    Ball(const std::string &color, double radius)
    {
        m_color = color;
        m_radius = radius;
    }

    void print()
    {
        std::cout << "color: " << m_color << ", radius: " << m_radius << '
';
    }
};

int main()
{
    Ball def;
    def.print();

    Ball black("black");
    black.print();

    Ball thirty(30.0);
    thirty.print();

    Ball blackThirty("black", 30.0);
    blackThirty.print();

    return 0;
}
```

#### Оптимізована версія (з параметрами за замовчуванням):
Для підтримки передачі `(radius)` та `(color)` окремо все одно зручно залишити два конструктори або конструктор з делегуванням:
```cpp
class Ball
{
private:
    std::string m_color = "red";
    double m_radius = 20.0;

public:
    Ball(double radius) : m_radius(radius) {}
    Ball(const std::string &color = "red", double radius = 20.0)
        : m_color(color), m_radius(radius) {}

    void print()
    {
        std::cout << "color: " << m_color << ", radius: " << m_radius << '
';
    }
};
```

---

## 2. Списки ініціалізації членів класу (Member Initializer Lists)

У тілі конструктора через оператор `=` виконується **присвоювання**, а не ініціалізація:
```cpp
class Something
{
private:
    int m_value1;
    double m_value2;
    char m_value3;

public:
    // Погано: створення з наступним переприсвоюванням
    Something()
    {
        m_value1 = 1;
        m_value2 = 2.2;
        m_value3 = 'c';
    }
};
```

### Чому список ініціалізації кращий?
1. **Константи (`const`) та посилання (`&`)**: Вони обов'язково мають бути ініціалізовані при створенні і не можуть бути змінені оператором `=` у тілі!
2. **Продуктивність**: Для полів, які є об'єктами інших класів, уникається зайвий виклик конструктора за замовчуванням з подальшим викликом оператора присвоювання.

```cpp
class Values
{
private:
    const int m_value; // константа!

public:
    // Правильно: ініціалізація через список
    Values(int value) : m_value(value)
    {
    }
};
```

### Ініціалізація масивів і вкладених класів у C++11
```cpp
class Something
{
private:
    const int m_array[5];

public:
    // C++11 uniform-ініціалізація для масивів у списку
    Something() : m_array{ 1, 2, 3, 4, 5 }
    {
    }
};
```

> **Важливе правило порядку:**  
> Змінні у списку ініціалізації ініціалізуються **в порядку їхнього оголошення в класі**, а НЕ в тому порядку, у якому вони перелічені в списку ініціалізації конструктора!

---

## 3. Ініціалізація нестатичних членів за замовчуванням (C++11)

Починаючи з C++11, полям класу можна надавати значення безпосередньо при їхньому оголошенні в тілі класу:
```cpp
class Values
{
private:
    int m_value1 = 1;     // пряма ініціалізація за замовчуванням
    double m_value2{ 2.2 };
    const int m_value3 = 3;

public:
    Values() {} // використовує 1, 2.2, 3
    Values(int v1) : m_value1(v1) {} // m_value1 перевизначено, решта за замовчуванням
};
```

---

## 4. Делегуючі конструктори (Constructor Delegation)

У C++11 конструктор може викликати інший конструктор того самого класу у списку ініціалізації. Це дозволяє уникнути дублювання коду без необхідності створювати допоміжний метод `init()`.

```cpp
#include <iostream>
#include <string>

class Employee
{
private:
    int m_id;
    std::string m_name;

public:
    // Базовий конструктор, що ініціалізує всі поля
    Employee(int id, const std::string &name)
        : m_id(id), m_name(name)
    {
        std::cout << "Employee " << m_name << " створений.
";
    }

    // Делегуючий конструктор 1
    Employee(int id) : Employee(id, "")
    {
    }

    // Делегуючий конструктор 2
    Employee(const std::string &name) : Employee(0, name)
    {
    }
};

int main()
{
    Employee emp1(101, "Іван");
    Employee emp2(102);
    Employee emp3("Олена");
    return 0;
}
```

> **Обмеження:** Конструктор, який делегує виконання іншому конструктору, **не може одночасно ініціалізувати інші поля** у своєму списку ініціалізації (хоча може містити код у фігурних дужках тіла).

---

## 5. Деструктори та ідіома RAII

**Деструктор** — це особливий метод класу, який автоматично викликається при знищенні об'єкта (виході зі scope або використанні `delete`).
- Назва деструктора збігається з назвою класу, але перед нею ставиться тильда `~`.
- Деструктор не приймає параметрів і нічого не повертає.
- У класі може бути лише один деструктор.

### Принцип RAII (Resource Acquisition Is Initialization)
«Захоплення ресурсу є ініціалізацією»: ресурс (динамічна пам'ять, файл, мережеве з'єднання, mutex) захоплюється в конструкторі та гарантовано звільняється в деструкторі.

```cpp
#include <iostream>

class Another
{
private:
    int m_nID;

public:
    Another(int nID) : m_nID(nID)
    {
        std::cout << "Створення об'єкта Another " << m_nID << '
';
    }

    ~Another()
    {
        std::cout << "Знищення об'єкта Another " << m_nID << '
';
    }
};

int main()
{
    // Виділяємо об'єкт зі стеку
    Another object1(1);

    {
        Another object2(2);
    } // object2 знищується тут при виході з блоку

    // Динамічний об'єкт
    Another *pObject = new Another(3);
    delete pObject; // object3 знищується тут

    return 0;
} // object1 знищується тут
```

> **Увага:** Функція `exit()` негайно завершує програму, при цьому локальні деструктори стекових об'єктів **не викликаються**!

---

## 6. Прихований покажчик `this`

Усі нестатичні методи класу неявно отримують перший прихований параметр: покажчик на об'єкт, для якого викликано метод — `ClassType* const this`.

```cpp
class BankAccount {
private:
    std::string accountNumber;
    std::string ownerName;
    double balance;

public:
    // Конструктор: явний this вирішує конфлікт імен між параметрами та полями
    BankAccount(std::string accountNumber, std::string ownerName, double balance) {
        this->accountNumber = accountNumber;
        this->ownerName = ownerName;
        this->balance = balance;
    }

    // Повернення посилання на поточний об'єкт (*this) для ланцюжкових викликів (chaining)
    BankAccount& deposit(double amount) {
        if (amount > 0) {
            this->balance += amount;
        }
        return *this;
    }
};
```

---

## 7. Розподіл класу на `.h` та `.cpp` файли

Для підтримки великих проєктів та чистоти архітектури класи поділяють:
- **Заголовковий файл (`.h`)**: містить визначення класу, оголошення полів та прототипи методів (відкритий інтерфейс).
- **Файл реалізації (`.cpp`)**: містить визначення методів класу з оператором `::`.

```cpp
// Date.h
#ifndef DATE_H
#define DATE_H

class Date
{
private:
    int m_day;
    int m_month;
    int m_year;

public:
    Date(int day, int month, int year);

    void setDate(int day, int month, int year);
    int getDay() const { return m_day; } // короткі методи можуть бути inline в .h
    void print() const;
};

#endif
```

```cpp
// Date.cpp
#include "Date.h"
#include <iostream>

Date::Date(int day, int month, int year)
{
    setDate(day, month, year);
}

void Date::setDate(int day, int month, int year)
{
    m_day = day;
    m_month = month;
    m_year = year;
}

void Date::print() const
{
    std::cout << m_day << '/' << m_month << '/' << m_year << '
';
}
```

---

## 8. Класи та `const`

### Константні об'єкти
Об'єкт, оголошений як `const Date today(2026, 9, 16);`, не дозволяє модифікацію своїх полів.

### Константні методи (const member functions)
Константний об'єкт має право викликати **тільки константні методи**.
Константний метод гарантує, що не змінюватиме поля об'єкта:
```cpp
class Date
{
private:
    int m_day;
    int m_month;
    int m_year;

public:
    Date(int day, int month, int year) : m_day(day), m_month(month), m_year(year) {}

    // Константний метод (const після списку параметрів)
    int getDay() const { return m_day; }

    // Неконстантний метод: викликати для const об'єктів заборонено
    void setDay(int day) { m_day = day; }
};
```

> **Правило:** Робіть будь-який метод константним (`const`), якщо він не змінює стан об'єкта! Це дозволяє безпечно передавати об'єкти за константним посиланням `const MyClass&`.

---

## 9. Статичні члени класу (`static`)

Статичні члени належать **самому класу**, а не його окремим екземплярам. Вони існують в єдиному екземплярі для всіх об'єктів класу.

### Статичні поля
- Оголошуються в класі як `static int s_idGenerator;`.
- Визначаються та ініціалізуються **в глобальній області видимості (.cpp файлі)**:
```cpp
#include <iostream>

class IDGenerator
{
private:
    static int s_nextID; // спільний лічильник

public:
    static int getNextID() { return s_nextID++; }
};

// Визначення статичного поля поза тілом класу:
int IDGenerator::s_nextID = 1;

int main()
{
    std::cout << IDGenerator::getNextID() << '
'; // 1
    std::cout << IDGenerator::getNextID() << '
'; // 2
    return 0;
}
```

### Статичні методи
- Не мають прихованого покажчика `this`.
- Можуть викликатися напряму без створення об'єкта: `ClassName::staticMethod()`.
- Мають доступ **лише до статичних полів та інших статичних методів** (не мають прямого доступу до звичайних нестатичних полів).

---

## 10. Конструктор копіювання (Copy Constructor)

Конструктор копіювання ініціалізує новий об'єкт копією вже існуючого об'єкта того ж класу:
```cpp
ClassName(const ClassName &source);
```

```cpp
#include <iostream>
#include <cassert>

class Drob
{
private:
    int m_numerator;
    int m_denominator;

public:
    Drob(int numerator = 0, int denominator = 1)
        : m_numerator(numerator), m_denominator(denominator)
    {
        assert(denominator != 0);
    }

    // Явний конструктор копіювання
    Drob(const Drob &drob)
        : m_numerator(drob.m_numerator), m_denominator(drob.m_denominator)
    {
        std::cout << "Виклик конструктора копіювання!
";
    }

    void print() const
    {
        std::cout << m_numerator << '/' << m_denominator << '
';
    }
};

int main()
{
    Drob d1(3, 5);
    Drob d2 = d1;     // Копіююча ініціалізація -> виклик конструктора копіювання
    Drob d3(d1);      // Пряма копіююча ініціалізація
    return 0;
}
```

### Запобігання копіюванню
Якщо клас не повинен копіюватися (наприклад, унікальний ресурс):
1. **У сучасному C++ (C++11)**: `Drob(const Drob &drob) = delete;`
2. **У класичному C++**: помістити конструктор копіювання в секцію `private:` без тіла.

---

## 11. Дружні функції та класи (`friend`)

Ключове слово `friend` дає зовнішній функції або іншому класу повний доступ до `private` та `protected` членів класу, зберігаючи їх закритими для решти коду.

### 1. Дружня функція
```cpp
#include <iostream>

class Anything
{
private:
    int m_value;

public:
    Anything() : m_value(0) {}

    // Оголошуємо дружню функцію
    friend void reset(Anything &a);
};

// Звичайна зовнішня функція, яка має доступ до m_value
void reset(Anything &a)
{
    a.m_value = 0;
}
```

### 2. Дружній клас
```cpp
class Values
{
private:
    int m_intValue;
    double m_doubleValue;

public:
    Values(int intVal, double dVal) : m_intValue(intVal), m_doubleValue(dVal) {}

    // Робимо клас Display другом класу Values
    friend class Display;
};

class Display
{
private:
    bool m_displayIntFirst;

public:
    Display(bool displayIntFirst) : m_displayIntFirst(displayIntFirst) {}

    void displayItem(const Values &value)
    {
        if (m_displayIntFirst)
            std::cout << value.m_intValue << " " << value.m_doubleValue << '
';
        else
            std::cout << value.m_doubleValue << " " << value.m_intValue << '
';
    }
};
```

### 3. Окремий дружній метод
Якщо дружнім потрібно зробити лише один конкретний метод іншого класу, потрібен чіткий порядок оголошень із винесенням визначення методу після повного опису обох класів (файл `19.cpp`).
