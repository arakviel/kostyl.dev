# ООП в C++: Перевантаження операторів, явні конструктори, анонімні об'єкти та глибоке копіювання

> **Викладач:** Верещагін О.О.  
> **Тема:** Анонімні об'єкти, вкладені типи, вимірювання часу виконання, перевантаження арифметичних операторів, операторів вводу/виводу (`<<`, `>>`), унарних операторів, операторів порівняння, інкременту/декременту (`++`, `--`), індексації (`[]`), виклику функції (`()`, функтори), перевантаження приведення типів, ключові слова `explicit` та `delete`, оператор присвоювання, поверхневе та глибоке копіювання.

---

## 1. Анонімні об'єкти (Anonymous Objects)

**Анонімний об'єкт** — це тимчасове значення, яке не має імені та створюється безпосередньо у виразі:
```cpp
Dollars(5); // Створює безіменний тимчасовий об'єкт класу Dollars
```
- Вони знищуються одразу після обчислення виразу, в якому були створені.
- Їх зручно використовувати для передачі аргументів у функції або повернення результатів, не створюючи зайвих іменованих змінних.

---

## 2. Вкладені типи даних у класах (Nested Types)

Усередині класу можна оголошувати власні перерахування (`enum`), структури, класи або псевдоніми типів (`using` / `typedef`). Це запобігає забрудненню глобального простору імен.

```cpp
#include <iostream>

class Fruit
{
public:
    // Перерахування FruitType інкапсульоване всередині класу
    enum FruitType
    {
        APPLE,
        BANANA,
        CHERRY
    };

private:
    FruitType m_type;
    int m_percentageEaten = 0;

public:
    Fruit(FruitType type) : m_type(type) {}
    FruitType getType() const { return m_type; }
};

int main()
{
    // Доступ до вкладеного типу через оператор видимості ::
    Fruit apple(Fruit::APPLE);
    return 0;
}
```

---

## 3. Вимірювання часу виконання коду (Таймінг за допомогою `std::chrono`)

Для профілювання швидкодії створюють допоміжний RAII-клас `Timer`, що використовує бібліотеку `<chrono>`:

```cpp
#include <iostream>
#include <chrono>

class Timer
{
private:
    using clock_t = std::chrono::high_resolution_clock;
    using second_t = std::chrono::duration<double, std::ratio<1>>;

    std::chrono::time_point<clock_t> m_beg;

public:
    Timer() : m_beg(clock_t::now()) {}

    void reset()
    {
        m_beg = clock_t::now();
    }

    double elapsed() const
    {
        return std::chrono::duration_cast<second_t>(clock_t::now() - m_beg).count();
    }
};
```

---

## 4. Перевантаження операторів: Фундаментальні правила

У C++ оператори насправді є синтаксичним цукром для функцій. Вираз `a + b` обробляється компілятором як `operator+(a, b)` або `a.operator+(b)`.

### Обмеження:
1. Не можна створювати нові оператори (наприклад, `**`).
2. Не можна змінювати арність (кількість операндів) або пріоритет операторів.
3. Хоча б один з операндів обов'язково повинен бути користувацького типу (не можна перевизначити `int + int`).
4. **Оператори, які НЕ МОЖНА перевантажувати**:
   - `::` (дозвіл області видимості);
   - `.` (вибір члена об'єкта);
   - `.*` (вибір члена через покажчик);
   - `?:` (тернарний умовний оператор);
   - `sizeof`, `typeid`.

### Три способи перевантаження:
1. **Через дружні функції (`friend`)** або **звичайні вільні функції**.
2. **Через методи класу (member functions)**.

---

## 5. Перевантаження через дружні функції

Найкраще підходить для операторів, які не модифікують свої операнди (бінарні арифметичні оператори `+`, `-`, `*`, `/`), а також коли лівий операнд не є об'єктом класу (наприклад, `5 + dollars`).

### Приклад класу `Dollars`:
```cpp
#include <iostream>

class Dollars
{
private:
    int m_dollars;

public:
    Dollars(int dollars) : m_dollars(dollars) {}

    int getDollars() const { return m_dollars; }

    // Dollars + Dollars
    friend Dollars operator+(const Dollars &d1, const Dollars &d2);

    // Dollars + int
    friend Dollars operator+(const Dollars &d1, int value);

    // int + Dollars (забезпечує симетрію)
    friend Dollars operator+(int value, const Dollars &d1);
};

Dollars operator+(const Dollars &d1, const Dollars &d2)
{
    return Dollars(d1.m_dollars + d2.m_dollars);
}

Dollars operator+(const Dollars &d1, int value)
{
    return Dollars(d1.m_dollars + value);
}

Dollars operator+(int value, const Dollars &d1)
{
    return Dollars(d1.m_dollars + value);
}
```

---

## 6. Перевантаження операторів вводу (`>>`) та виводу (`<<`)

Оператори `<<` та `>>` **завжди** перевантажуються як зовнішні (дружні) функції, оскільки лівий операнд є потоком (`std::ostream&` або `std::istream&`), а не нашим класом!

```cpp
#include <iostream>

class Point
{
private:
    double m_x;
    double m_y;
    double m_z;

public:
    Point(double x = 0.0, double y = 0.0, double z = 0.0)
        : m_x(x), m_y(y), m_z(z) {}

    // Вивід у потік (передаємо та повертаємо потік за посиланням для ланцюжкових викликів)
    friend std::ostream& operator<<(std::ostream &out, const Point &point)
    {
        out << "Point(" << point.m_x << ", " << point.m_y << ", " << point.m_z << ")";
        return out;
    }

    // Ввід з потоку
    friend std::istream& operator>>(std::istream &in, Point &point)
    {
        in >> point.m_x >> point.m_y >> point.m_z;
        return in;
    }
};
```

---

## 7. Перевантаження через методи класу

Коли оператор перевантажується як метод класу:
- Лівий операнд стає неявним об'єктом `*this`.
- Список параметрів методу приймає на один операнд менше!

```cpp
class Dollars
{
private:
    int m_dollars;

public:
    Dollars(int dollars) : m_dollars(dollars) {}

    // Dollars - Dollars через метод класу (приймає лише правий операнд)
    Dollars operator-(const Dollars &d2) const
    {
        return Dollars(m_dollars - d2.m_dollars);
    }
};
```

### Що ОБОВ'ЯЗКОВО перевантажується ТІЛЬКИ через методи класу:
- Оператор присвоювання `=`;
- Оператор індексації `[]`;
- Оператор виклику функції `()`;
- Оператор доступу до членів через вказівник `->`.

---

## 8. Перевантаження унарних операторів та операторів порівняння

### Унарний мінус і логічне НЕ (`!`)
```cpp
class Something
{
private:
    double m_a;
    double m_b;

public:
    Something(double a = 0.0, double b = 0.0) : m_a(a), m_b(b) {}

    // Унарний мінус
    Something operator-() const
    {
        return Something(-m_a, -m_b);
    }

    // Логічне НЕ
    bool operator!() const
    {
        return (m_a == 0.0 && m_b == 0.0);
    }
};
```

### Оператори порівняння (`==`, `!=`, `<`, `>`)
Достатньо реалізувати `==` та `<`, а інші оператори виразити через них:
```cpp
bool operator!=(const Car &c1, const Car &c2)
{
    return !(c1 == c2);
}
```

---

## 9. Префіксний і постфіксний інкремент/декремент (`++`, `--`)

Щоб розрізнити префіксну та постфіксну форми:
- **Префіксна форма (`++a`)**: не приймає аргументів, повертає `*this` по посиланню (`Type&`).
- **Постфіксна форма (`a++`)**: приймає фіктивний аргумент `int`, повертає копію стану до збільшення по значенню (`Type`).

```cpp
class Number
{
private:
    int m_num;

public:
    Number(int num = 0) : m_num(num) {}

    // Префікс (++n)
    Number& operator++()
    {
        ++m_num;
        return *this;
    }

    // Постфікс (n++)
    Number operator++(int)
    {
        Number temp(*this); // зберігаємо копію
        ++(*this);          // викликаємо префіксний варіант
        return temp;        // повертаємо збережений попередній стан
    }
};
```

---

## 10. Оператор індексації `[]`

Оператор `[]` повинен повертати елемент за посиланням (`&`), щоб його можна було використовувати як l-value (зліва від `=`):

```cpp
#include <cassert>

class IntArray
{
private:
    int m_data[10];

public:
    // Для неконстантних об'єктів (дозволяє запис)
    int& operator[](int index)
    {
        assert(index >= 0 && index < 10);
        return m_data[index];
    }

    // Для константних об'єктів (тільки читання)
    const int& operator[](int index) const
    {
        assert(index >= 0 && index < 10);
        return m_data[index];
    }
};
```

---

## 11. Оператор виклику функції `()` та функтори

Перевантаження оператора `()` дозволяє викликати об'єкт як функцію. Такі об'єкти називаються **функторами** (function objects). Головна перевага над звичайними функціями — функтор може зберігати внутрішній стан між викликами!

```cpp
#include <iostream>

class Accumulator
{
private:
    int m_counter = 0;

public:
    // Перевантаження оператора ()
    int operator()(int i)
    {
        return (m_counter += i);
    }
};

int main()
{
    Accumulator acc;
    std::cout << acc(10) << '
'; // 10
    std::cout << acc(20) << '
'; // 30
    return 0;
}
```

Також `operator()` використовують для індексації багатовимірних масивів: `matrix(row, col)`.

---

## 12. Перевантаження операцій приведення типів (User-defined Typecast)

Дозволяє класу автоматично перетворюватися на інший тип (наприклад, `int`, `double` тощо):
```cpp
class Dollars
{
private:
    int m_dollars;

public:
    Dollars(int dollars = 0) : m_dollars(dollars) {}

    // Конвертація в Cents (не вказується тип повернення перед назвою!)
    operator int() const { return m_dollars; }
};
```

---

## 13. Ключові слова `explicit` та `delete`

### `explicit` (заборона неявної конвертації)
За замовчуванням будь-який конструктор з одним параметром розглядається компілятором як оператор неявної конвертації. Ключове слово `explicit` забороняє таку неявну підстановку:

```cpp
class Mystring
{
public:
    explicit Mystring(int length); // тільки явна ініціалізація
    Mystring(const char *str);
};

void printString(const Mystring &s);

// printString('x'); // Помилка компіляції при наявності explicit! Захищає від багів.
```

### `delete` (заборона виклику функції)
У C++11 можна явно заборонити створення певних викликів або перевантажень:
```cpp
class Mystring
{
public:
    Mystring(char) = delete; // забороняємо створення з char
};
```

---

## 14. Поверхневе та глибоке копіювання, оператор присвоювання `=`

### Проблема поверхневого копіювання (Shallow Copy)
Конструктор копіювання та оператор присвоювання за замовчуванням копіюють дані побайтово (почленно). Якщо в класі є динамічний вказівник (`new int[...]` або `char*`), копіюється лише адреса, що призводить до подвійного видалення пам'яті (`double free`) та висячих покажчиків при знищенні об'єктів!

### Глибоке копіювання (Deep Copy)
Виділяє власний новий блок динамічної пам'яті та копіює туди фактичні дані:

```cpp
#include <iostream>
#include <cstring>

class SomeString
{
private:
    char *m_data;
    int m_length;

public:
    SomeString(const char *source = "")
    {
        m_length = std::strlen(source) + 1;
        m_data = new char[m_length];
        strcpy_s(m_data, m_length, source);
    }

    // Деструктор
    ~SomeString()
    {
        delete[] m_data;
    }

    // Конструктор глибокого копіювання
    SomeString(const SomeString &source)
    {
        m_length = source.m_length;
        if (source.m_data)
        {
            m_data = new char[m_length];
            strcpy_s(m_data, m_length, source.m_data);
        }
        else
            m_data = nullptr;
    }

    // Перевантаження оператора присвоювання з захистом від самоприсвоювання
    SomeString& operator=(const SomeString &source)
    {
        // 1. Перевірка на самоприсвоювання (self-assignment check)
        if (this == &source)
            return *this;

        // 2. Звільнення попередньої пам'яті
        delete[] m_data;

        // 3. Виділення нової пам'яті та копіювання даних
        m_length = source.m_length;
        if (source.m_data)
        {
            m_data = new char[m_length];
            strcpy_s(m_data, m_length, source.m_data);
        }
        else
            m_data = nullptr;

        // 4. Повернення посилання на поточний об'єкт
        return *this;
    }
};
```

> **Правило трьох (Rule of Three):** Якщо вашому класу потрібен власний деструктор, конструктор копіювання або перевантажений оператор присвоювання, йому майже напевно потрібні **всі три**!
